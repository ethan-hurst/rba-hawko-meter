---
phase: 11-test-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - tests/python/conftest.py
  - tests/python/fixtures/abs_cpi.csv
  - tests/python/fixtures/abs_employment.csv
  - tests/python/fixtures/abs_wage_price_index.csv
  - tests/python/fixtures/abs_household_spending.csv
  - tests/python/fixtures/abs_building_approvals.csv
  - tests/python/fixtures/corelogic_housing.csv
  - tests/python/fixtures/nab_capacity.csv
  - tests/python/test_smoke.py
autonomous: true
requirements:
  - FOUND-02
  - FOUND-03

must_haves:
  truths:
    - "A test that imports pipeline.config sees DATA_DIR pointing to a tmp_path directory, not the live data/ folder"
    - "A test that calls socket.socket() raises RuntimeError with the message 'Network access blocked in tests'"
    - "A test decorated with @pytest.mark.live is NOT blocked by the network fixture"
    - "pytest tests/python/ -m 'not live' discovers and runs smoke tests, exits 0"
    - "pytest tests/python/ -m 'not live' with a live-marked test skips it; -m live includes it"
    - "Fixture CSVs contain real historical data with correct column headers matching production format"
  artifacts:
    - path: "tests/python/conftest.py"
      provides: "Autouse DATA_DIR isolation, network blocker, fixture CSV loaders"
      contains: "isolate_data_dir"
    - path: "tests/python/conftest.py"
      provides: "Socket-level network blocker with live marker escape"
      contains: "block_network"
    - path: "tests/python/test_smoke.py"
      provides: "Infrastructure smoke tests proving all autouse fixtures work"
      contains: "test_data_dir_isolated"
    - path: "tests/python/fixtures/abs_cpi.csv"
      provides: "CPI fixture data (~40 rows)"
      contains: "date,value,source,series_id"
    - path: "tests/python/fixtures/nab_capacity.csv"
      provides: "NAB capacity fixture data"
      contains: "date,value,source"
  key_links:
    - from: "tests/python/conftest.py"
      to: "pipeline.config.DATA_DIR"
      via: "monkeypatch.setattr in isolate_data_dir"
      pattern: 'monkeypatch\.setattr\(pipeline\.config.*DATA_DIR'
    - from: "tests/python/conftest.py"
      to: "socket.socket"
      via: "monkeypatch.setattr in block_network"
      pattern: 'monkeypatch\.setattr\(socket.*socket'
    - from: "tests/python/test_smoke.py"
      to: "tests/python/conftest.py"
      via: "autouse fixtures auto-applied"
      pattern: "pipeline\\.config\\.DATA_DIR"
---

<objective>
Create the test infrastructure: conftest.py with autouse fixtures (DATA_DIR isolation, network blocker), fixture CSVs with real historical data, and smoke tests proving the infrastructure works.

Purpose: Deliver the isolated test harness that all future Python tests (Phase 12+) will build on. Smoke tests prove the three core guarantees: data isolation, network blocking, and tier separation.

Output: `tests/python/conftest.py`, 7 fixture CSVs in `tests/python/fixtures/`, and `tests/python/test_smoke.py`.
</objective>

<execution_context>
@/Users/annon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/annon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-test-foundation/11-RESEARCH.md
@.planning/phases/11-test-foundation/11-01-SUMMARY.md
@pipeline/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create conftest.py with autouse fixtures and CSV loaders</name>
  <files>tests/python/conftest.py</files>
  <action>
Create `tests/python/conftest.py` (create `tests/python/` directory first). This file provides all shared test infrastructure.

**FIXTURES_DIR constant:**
```python
FIXTURES_DIR = Path(__file__).parent / "fixtures"
```
Use `Path(__file__).parent` to avoid CWD sensitivity (per research pitfall #4).

**Autouse fixture 1: `isolate_data_dir` (FOUND-02)**
- Scope: function (default — required for monkeypatch compatibility)
- `autouse=True` — every test gets DATA_DIR isolation automatically
- Implementation: `monkeypatch.setattr(pipeline.config, "DATA_DIR", tmp_path)`
- Import `pipeline.config` as a module (NOT `from pipeline.config import DATA_DIR` — that creates an unpatchable local binding, per research anti-pattern #3)
- Docstring: explain that this prevents tests from reading/writing the live `data/` folder
- Note in comment: `SOURCE_METADATA` paths are NOT retroactively updated; Phase 12 tests using SOURCE_METADATA will need additional patching

**Autouse fixture 2: `block_network` (FOUND-02 network safety)**
- Scope: function (default)
- `autouse=True` — all tests get network blocking by default
- Check `request.node.get_closest_marker("live")` — if marker present, `yield` and `return` (step aside for live tests)
- For non-live tests: define inner function `blocked_socket(*args, **kwargs)` that raises `RuntimeError("Network access blocked in tests. Use @pytest.mark.live for tests requiring network.")`
- Monkeypatch: `monkeypatch.setattr(socket, "socket", blocked_socket)` — blocks ALL outbound connections at OS socket level (catches requests, urllib, httpx, anything)
- Block everything including localhost — no exceptions for non-live tests (per user decision)

**Named fixtures for CSV loading (FOUND-03):**
Create one fixture per indicator CSV, returning a pandas DataFrame:
- `fixture_cpi_df` → reads `FIXTURES_DIR / "abs_cpi.csv"`
- `fixture_employment_df` → reads `FIXTURES_DIR / "abs_employment.csv"`
- `fixture_wages_df` → reads `FIXTURES_DIR / "abs_wage_price_index.csv"`
- `fixture_spending_df` → reads `FIXTURES_DIR / "abs_household_spending.csv"`
- `fixture_building_approvals_df` → reads `FIXTURES_DIR / "abs_building_approvals.csv"`
- `fixture_housing_df` → reads `FIXTURES_DIR / "corelogic_housing.csv"`
- `fixture_nab_capacity_df` → reads `FIXTURES_DIR / "nab_capacity.csv"`

All use `pd.read_csv(FIXTURES_DIR / "filename.csv")`. These are NOT autouse — tests request them explicitly when needed.

Imports needed at top: `socket`, `pathlib.Path`, `pandas`, `pytest`, `pipeline.config`.
  </action>
  <verify>
    <automated>cd /Users/annon/projects/rba-hawko-meter && python -c "import ast; ast.parse(open('tests/python/conftest.py').read()); print('OK: conftest.py is valid Python')"</automated>
    <manual>Verify conftest.py has isolate_data_dir and block_network autouse fixtures, plus 7 CSV loader fixtures</manual>
  </verify>
  <done>conftest.py exists at tests/python/conftest.py with: (1) FIXTURES_DIR using Path(__file__).parent, (2) isolate_data_dir autouse fixture patching pipeline.config.DATA_DIR to tmp_path, (3) block_network autouse fixture patching socket.socket with live-marker escape hatch, (4) 7 named CSV loader fixtures returning DataFrames.</done>
</task>

<task type="auto">
  <name>Task 2: Create fixture CSVs from production data and smoke tests</name>
  <files>
    tests/python/fixtures/abs_cpi.csv
    tests/python/fixtures/abs_employment.csv
    tests/python/fixtures/abs_wage_price_index.csv
    tests/python/fixtures/abs_household_spending.csv
    tests/python/fixtures/abs_building_approvals.csv
    tests/python/fixtures/corelogic_housing.csv
    tests/python/fixtures/nab_capacity.csv
    tests/python/test_smoke.py
  </files>
  <action>
**Part A: Create fixture CSVs (FOUND-03)**

Create `tests/python/fixtures/` directory. Copy real historical data from `data/*.csv` production files, trimmed to ~40 rows each. These are real snapshots, NOT synthetic data (per user decision).

For each CSV:
1. Read the production file from `data/`
2. Take the first ~40 rows (preserving header) — enough for 10-year rolling window on quarterly data
3. Write to `tests/python/fixtures/` with the EXACT same column headers as production

Files to create (column format noted):
- `abs_cpi.csv` — `date,value,source,series_id` — ~40 rows (quarterly data from 2014)
- `abs_employment.csv` — `date,value,source,series_id` — ~48 rows (monthly, 4 years)
- `abs_wage_price_index.csv` — `date,value,source,series_id` — ~40 rows (quarterly, 10 years)
- `abs_household_spending.csv` — `date,value,source,series_id` — ~48 rows (monthly, 4 years)
- `abs_building_approvals.csv` — `date,value,source,series_id` — ~48 rows (monthly, 4 years)
- `corelogic_housing.csv` — `date,value,source,series_id` — ~40 rows (quarterly)
- `nab_capacity.csv` — `date,value,source` (NO series_id column) — all available rows (currently only ~7 rows in production)

Use a shell command to copy and trim: `head -41 data/file.csv > tests/python/fixtures/file.csv` (41 = 1 header + 40 data rows). For NAB, copy the entire file since it has fewer than 40 rows.

**Part B: Create smoke tests (tests/python/test_smoke.py)**

Create `tests/python/test_smoke.py` with 5 smoke tests that prove the infrastructure works:

1. `test_pytest_discovers_and_exits_zero()` — baseline: `assert True`. Proves pytest discovery is wired.

2. `test_data_dir_isolated_from_production()` — verify DATA_DIR is NOT the live `data/` folder:
   ```python
   assert str(pipeline.config.DATA_DIR) != str(Path("data"))
   assert "tmp" in str(pipeline.config.DATA_DIR).lower() or str(pipeline.config.DATA_DIR).startswith("/")
   ```
   Import `pipeline.config` and check `DATA_DIR` is a tmp_path, not the relative `Path("data")`.

3. `test_network_blocker_raises_runtime_error()` — verify socket blocking:
   ```python
   with pytest.raises(RuntimeError, match="Network access blocked"):
       socket.socket()
   ```

4. `test_live_marker_exempts_network_block()` — decorated with `@pytest.mark.live`:
   ```python
   s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
   s.close()
   ```
   This MUST NOT raise. Proves the autouse fixture steps aside for live-marked tests.

5. `test_fixture_csvs_loadable()` — request `fixture_cpi_df` fixture, check it's a DataFrame with >0 rows and has `date` and `value` columns. Proves fixture CSV loading works.

Imports needed: `socket`, `pathlib.Path`, `pytest`, `pipeline.config`.
  </action>
  <verify>
    <automated>cd /Users/annon/projects/rba-hawko-meter && pytest tests/python/ -m "not live" -v 2>&1 | tail -20</automated>
    <manual>Verify: 4 non-live tests pass, 1 live test is deselected. Fixture CSVs exist with correct headers.</manual>
  </verify>
  <done>
7 fixture CSVs in tests/python/fixtures/ with real production data (~40 rows each, correct column headers). test_smoke.py has 5 tests: (1) baseline discovery, (2) DATA_DIR isolation verified, (3) network blocker raises RuntimeError, (4) live marker exempts network block, (5) fixture CSV loads as DataFrame. Running `pytest tests/python/ -m "not live"` passes 4 tests with 1 deselected (live). Running `pytest tests/python/ -m live` runs the live-marked test.
  </done>
</task>

</tasks>

<verification>
1. `pytest tests/python/ -m "not live" -v` — discovers 4 tests, all pass, 1 deselected (live-marked), exit code 0
2. `pytest tests/python/ -m live -v` — discovers 1 test (live marker), passes, exit code 0
3. `pytest tests/python/ -v` — discovers all 5 tests, all pass, exit code 0
4. `python -c "import pipeline.config; print(pipeline.config.DATA_DIR)"` from project root prints `data` (production is untouched)
5. `ls tests/python/fixtures/*.csv | wc -l` returns 7
6. `head -1 tests/python/fixtures/abs_cpi.csv` returns `date,value,source,series_id`
7. `head -1 tests/python/fixtures/nab_capacity.csv` returns `date,value,source` (no series_id)
</verification>

<success_criteria>
- conftest.py provides autouse DATA_DIR isolation (monkeypatched to tmp_path) and network blocking (socket.socket raises RuntimeError)
- Network blocker respects @pytest.mark.live — live tests are exempted
- 7 fixture CSVs with real production data snapshots exist in tests/python/fixtures/
- 5 smoke tests prove: discovery works, data is isolated, network is blocked, live marker escapes blocker, fixtures load
- `pytest tests/python/ -m "not live"` exits 0 with 4 passed, 1 deselected
- Tier separation confirmed: `-m live` and `-m "not live"` correctly partition tests
</success_criteria>

<output>
After completion, create `.planning/phases/11-test-foundation/11-02-SUMMARY.md`
</output>
