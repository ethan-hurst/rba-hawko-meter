---
phase: 08-asx-futures-live-data
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - public/js/interpretations.js
  - tests/dashboard.spec.js
autonomous: true
requirements:
  - ASX-02
  - ASX-04

must_haves:
  truths:
    - "Dashboard 'What Markets Expect' section shows a table with rows for the next 3-4 upcoming RBA meetings"
    - "Each meeting row shows the full date, implied rate, basis-point change, and a stacked traffic-light probability bar"
    - "The first (soonest) meeting row is visually highlighted with border-l accent"
    - "Traffic light colors are green (cut), amber (hold), red (hike) — replacing old blue/gray/red"
    - "'Data as of [date]' footer is always visible below the table"
    - "When ASX data is null or meetings array is empty, section shows 'Market futures data currently unavailable' placeholder instead of being hidden"
    - "Test 7 updated to expect visible placeholder instead of hidden container"
  artifacts:
    - path: "public/js/interpretations.js"
      provides: "Multi-meeting renderASXTable() with traffic light stacked bars"
      contains: "createStackedBar"
    - path: "tests/dashboard.spec.js"
      provides: "Updated test 7 for visible placeholder behavior"
      contains: "Market futures data currently unavailable"
  key_links:
    - from: "public/js/interpretations.js"
      to: "public/data/status.json"
      via: "renderASXTable reads asxData.meetings array"
      pattern: "asxData\\.meetings"
    - from: "public/js/interpretations.js"
      to: "public/js/gauge-init.js"
      via: "InterpretationsModule.renderASXTable called with status.json data"
      pattern: "renderASXTable"
    - from: "tests/dashboard.spec.js"
      to: "public/js/interpretations.js"
      via: "Test 6 injects meetings array, test 7 expects placeholder text"
      pattern: "meetings"
---

<objective>
Replace the single-meeting ASX probability table with a multi-meeting table using traffic light stacked bars.

Purpose: Users need to see market expectations for the next 3-4 RBA meetings at a glance, not just the soonest one. The traffic light color scheme (green/amber/red) makes probability splits immediately comprehensible. The section must always be visible (with placeholder if no data) per user decision.

Output: Rewritten `renderASXTable()` in interpretations.js producing a multi-meeting table with stacked bars, updated test assertions.
</objective>

<execution_context>
@/Users/annon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/annon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-asx-futures-live-data/08-RESEARCH.md
@.planning/phases/08-asx-futures-live-data/08-01-SUMMARY.md

@public/js/interpretations.js
@public/js/gauge-init.js
@tests/dashboard.spec.js
@public/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite renderASXTable() for multi-meeting display with traffic light bars</name>
  <files>
    public/js/interpretations.js
  </files>
  <action>
Replace the entire `renderASXTable()` function (lines 91-181 in current `interpretations.js`) with a new implementation. All DOM must use safe methods (`createElement`/`textContent` only — no `innerHTML`). Follow the existing IIFE pattern.

**New renderASXTable(containerId, asxData):**

1. **Always show section** (locked decision):
   - `container.textContent = ''` to clear
   - `container.style.display = ''` — ALWAYS visible, never `display: none`

2. **Heading** (unchanged):
   - `h3.text-lg.font-semibold.text-gray-200.mb-3` with text "What Markets Expect"

3. **Placeholder when no data** (locked decision):
   - If `!asxData || !asxData.meetings || asxData.meetings.length === 0`: render `p.text-sm.text-gray-500.italic` with text "Market futures data currently unavailable" and return
   - Do NOT check `asxData.probabilities` anymore — the new contract uses `asxData.meetings`

4. **Current cash rate reference** (discretion — include it):
   - If `asxData.current_rate != null`: render `p.text-xs.text-gray-400.mb-3` with text "Current cash rate: X.XX%"

5. **Subline** (existing):
   - `p.text-xs.text-gray-500.mb-3` with text "Based on ASX 30 Day Interbank Cash Rate Futures pricing"

6. **Table wrapper** for mobile scroll (locked decision):
   - `div.overflow-x-auto` wrapper around the table

7. **Table structure**:
   - `table.w-full.text-sm` with `style.minWidth = '480px'`
   - **thead**: single row with columns: "Meeting", "Implied Rate", "Change", "Probability" (all `th.text-left.py-2.text-gray-400.font-medium`, except "Probability" gets `style.minWidth = '180px'`)
   - **tbody**: one `tr` per meeting in `asxData.meetings`

8. **Meeting rows**:
   - First row (index 0) gets highlight: `tr` class includes `border-l-2 border-finance-accent` and add `style.backgroundColor = 'rgba(26, 26, 26, 0.5)'` (subtle highlight per locked decision)
   - All rows: `tr.border-b.border-finance-border/50`
   - **Meeting date cell**: `td.py-2.pl-2.text-gray-300` with `meeting.meeting_date_label` text (full date like "3 Mar 2026" per locked decision)
   - **Implied rate cell**: `td.py-2.text-gray-300.font-mono` with `meeting.implied_rate.toFixed(2) + '%'`
   - **Change cell**: `td.py-2.font-mono` — show `change_bp` with sign: `'+2.5bp'` or `'-25.0bp'`. Color: green (`#10b981`) if negative (cut), red (`#ef4444`) if positive (hike), gray (`#9ca3af`) if within +/-5. Use `(meeting.change_bp >= 0 ? '+' : '') + meeting.change_bp.toFixed(1) + 'bp'`
   - **Probability cell**: contains a stacked bar + percentage label below

9. **Stacked bar (per row)** — helper function `createStackedBar(probCut, probHold, probHike)`:
   - Container: `div.flex.rounded.overflow-hidden.w-full` with `style.height = '16px'` and `style.minWidth = '120px'`
   - Traffic light colors (locked decision): Cut = `#10b981` (green), Hold = `#f59e0b` (amber), Hike = `#ef4444` (red)
   - For each probability > 0, create a `div` child with `style.flex = String(probability)` and `style.backgroundColor = color`
   - Zero-width segments (discretion — omit): do NOT render segments where `probability === 0`

10. **Percentage labels below bar** (discretion — below, not inside):
    - `p.text-xs.text-gray-500.mt-1` — build condensed label showing only non-zero probabilities
    - Format: segments like "85% cut" / "15% hold" joined by " · " (middle dot separator)
    - Use the traffic light colors inline for each label segment: wrap each in a `span` with `style.color = <traffic light color>`

11. **Legend row** — add a small color legend below the table:
    - `div.flex.gap-4.mt-2.text-xs.text-gray-500`
    - Three items: green dot + "Cut", amber dot + "Hold", red dot + "Hike"
    - Each dot is a `span` with `display: inline-block; width: 8px; height: 8px; border-radius: 50%; background-color: <color>; margin-right: 4px; vertical-align: middle;`

12. **"Data as of" footer** (locked decision — always visible):
    - `p.text-xs.text-gray-500.mt-3` with text `'Data as of ' + formatAusDate(asxData.data_date)`
    - `formatAusDate` is already available in the module scope

**Also update the existing single-meeting probability table colors for consistency (locked decision):**
The old `renderASXTable` is being entirely replaced, so the old blue/gray/red colors are automatically removed. No separate color update needed.

**Important implementation notes:**
- Do NOT use `innerHTML` anywhere — the project prohibits it
- Use `textContent` for all text content
- The `ausDateFormatter` and `formatAusDate` function already exist in the module scope — reuse them
- The `percentFormatter` already exists — but for the bar labels, use simple `Math.round()` + '%' instead (cleaner for probability display)
- Keep all existing exports from the IIFE unchanged: `renderVerdict`, `renderASXTable`, `renderStalenessWarning`, `generateMetricInterpretation`, `renderMetricCard`, `getWhyItMatters`, `getPlainVerdict`, `formatAusDate`, `isDataSuspect`
  </action>
  <verify>
    <automated>cd /Users/annon/projects/rba-hawko-meter && node -e "
const fs = require('fs');
const src = fs.readFileSync('public/js/interpretations.js', 'utf8');

// Verify key patterns exist
const checks = [
  ['createStackedBar', 'Stacked bar helper function'],
  ['asxData.meetings', 'Multi-meeting data access'],
  ['#10b981', 'Traffic light green (cut)'],
  ['#f59e0b', 'Traffic light amber (hold)'],
  ['#ef4444', 'Traffic light red (hike)'],
  ['Market futures data currently unavailable', 'Placeholder text'],
  ['Data as of', 'Always-visible date footer'],
  ['overflow-x-auto', 'Mobile horizontal scroll wrapper'],
  ['border-finance-accent', 'First row highlight'],
  ['meeting_date_label', 'Full date format rendering'],
];

let pass = true;
for (const [pattern, desc] of checks) {
  if (!src.includes(pattern)) {
    console.log('FAIL: Missing ' + desc + ' (' + pattern + ')');
    pass = false;
  } else {
    console.log('OK: ' + desc);
  }
}

// Verify no innerHTML
if (src.includes('innerHTML')) {
  console.log('FAIL: innerHTML found — project prohibits it');
  pass = false;
} else {
  console.log('OK: No innerHTML usage');
}

// Verify old blue/gray/red colors removed
if (src.includes('#60a5fa') && src.includes('Rate Cut')) {
  console.log('FAIL: Old blue color still present in ASX table context');
  pass = false;
}

if (!pass) process.exit(1);
console.log('PASS: All renderASXTable checks passed');
"
    </automated>
    <manual>Open the dashboard in a browser. Verify the "What Markets Expect" section shows a multi-row table with traffic light bars. Check mobile viewport (375px) for horizontal scroll.</manual>
  </verify>
  <done>
    - `renderASXTable()` renders a multi-meeting table with rows for 3-4 upcoming RBA meetings
    - Each row has: full date label, implied rate %, basis-point change with color, stacked traffic-light bar
    - First meeting row is highlighted with `border-l-2 border-finance-accent`
    - Traffic light colors: green (#10b981) cut, amber (#f59e0b) hold, red (#ef4444) hike
    - Zero-probability segments are omitted from bars
    - "Data as of [date]" footer always visible
    - "Market futures data currently unavailable" shown when data is null/empty
    - No innerHTML used anywhere
    - Mobile horizontal scroll via overflow-x-auto wrapper
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Playwright tests for new ASX section behavior</name>
  <files>
    tests/dashboard.spec.js
  </files>
  <action>
**Update test 6 (ASX futures section renders with probability table when data available):**

The test currently injects a single-meeting `asx_futures` object. Update it to inject the new multi-meeting contract:

Replace the injected `json.asx_futures` object with:
```javascript
json.asx_futures = {
  current_rate: 4.35,
  implied_rate: 4.10,
  next_meeting: '2026-02-18',
  direction: 'cut',
  data_date: '2026-02-07',
  staleness_days: 0,
  probabilities: { cut: 85, hold: 15, hike: 0 },
  meetings: [
    {
      meeting_date: '2026-03-03',
      meeting_date_label: '3 Mar 2026',
      implied_rate: 4.10,
      change_bp: -25.0,
      probability_cut: 85,
      probability_hold: 15,
      probability_hike: 0
    },
    {
      meeting_date: '2026-04-07',
      meeting_date_label: '7 Apr 2026',
      implied_rate: 4.15,
      change_bp: -20.0,
      probability_cut: 60,
      probability_hold: 40,
      probability_hike: 0
    },
    {
      meeting_date: '2026-05-19',
      meeting_date_label: '19 May 2026',
      implied_rate: 4.20,
      change_bp: -15.0,
      probability_cut: 40,
      probability_hold: 60,
      probability_hike: 0
    }
  ]
};
```

Update the test assertions:
- Keep: `await expect(asxContainer).toBeVisible({ timeout: 15000 })`
- Keep: `await expect(asxContainer).toContainText('What Markets Expect')`
- Replace the old probability text assertions with multi-meeting assertions:
  - `await expect(asxContainer).toContainText('3 Mar 2026')` — first meeting date
  - `await expect(asxContainer).toContainText('7 Apr 2026')` — second meeting date
  - `await expect(asxContainer).toContainText('4.10%')` — implied rate for first meeting
  - `await expect(asxContainer).toContainText('Data as of')` — always-visible footer

**Update test 7 (ASX futures section hidden when data unavailable):**

Change the behavior assertion from hidden to visible with placeholder:
- Replace `await expect(asxContainer).toBeHidden()` with:
  - `await expect(asxContainer).toBeVisible()`
  - `await expect(asxContainer).toContainText('Market futures data currently unavailable')`

This matches the Phase 8 locked decision: section is always visible, shows placeholder when no data.
  </action>
  <verify>
    <automated>cd /Users/annon/projects/rba-hawko-meter && npx playwright test tests/dashboard.spec.js --reporter=line 2>&1 | tail -20</automated>
    <manual>Review test output to confirm tests 6 and 7 pass. If Playwright is not installed or the dev server is not running, verify the test file syntax is correct with `node -e "require('./tests/dashboard.spec.js')"` (will fail but should not have syntax errors).</manual>
  </verify>
  <done>
    - Test 6 injects multi-meeting data and asserts meeting dates, implied rates, and "Data as of" footer are visible
    - Test 7 asserts ASX container is VISIBLE (not hidden) with "Market futures data currently unavailable" placeholder text
    - All other tests (1-5) are unchanged and unaffected
  </done>
</task>

</tasks>

<verification>
1. `node -e "..."` static check confirms traffic light colors, stacked bar, mobile scroll, and placeholder text in interpretations.js
2. `npx playwright test tests/dashboard.spec.js` — all 7 tests pass (tests 6 and 7 updated for new behavior)
3. Visual check: dashboard shows multi-meeting table with colored stacked bars at desktop viewport
4. Visual check: dashboard shows horizontal scrollable table at 375px mobile viewport
5. Visual check: with null asx_futures data, section shows "Market futures data currently unavailable" instead of being hidden
</verification>

<success_criteria>
- "What Markets Expect" section displays a multi-meeting table with 3-4 rows
- Each row shows full date, implied rate, bp change, and stacked traffic-light probability bar
- First row is visually highlighted
- Section always visible — placeholder shown when data unavailable
- Traffic light colors (green/amber/red) replace old blue/gray/red
- All Playwright tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-asx-futures-live-data/08-02-SUMMARY.md`
</output>
