# Plan 19-02: test_ingest_rba.py + test_ingest_asx.py + test_ingest_corelogic.py + test_ingest_nab.py

**Phase:** 19 — Ingest Module Tests
**Scope:** Full test coverage for RBA, ASX, CoreLogic, and NAB ingest modules
**Requirements:** INGEST-02 (rba_data), INGEST-03 (asx_futures), INGEST-04 (corelogic), INGEST-05 (nab)
**Target:** 85%+ coverage per module

## Prerequisites

- Plan 19-01 complete (conftest fixtures, mock-session pattern established)

## Task 1: Create test_ingest_rba.py

Create `tests/python/test_ingest_rba.py` for rba_data.py (45 statements).

### Patch target: `pipeline.ingest.rba_data.create_session`

### _make_mock_session helper:
Same pattern as Plan 19-01 (copy into this file for self-containment).

### Test functions:

```
test_fetch_cash_rate_happy_path(fixture_rba_response)
    - Mock session.get returns fixture_rba_response as response.text
    - Verify returns DataFrame with columns [date, value, source]
    - Verify source = 'RBA'
    - Verify dates are ISO format (YYYY-MM-DD)
    - Verify values are numeric (4 data rows in fixture)

test_fetch_cash_rate_finds_header_row(fixture_rba_response)
    - Verify the header detection finds "Series ID" row
    - Verify the metadata rows (Title, Description, etc.) are skipped

test_fetch_cash_rate_http_error
    - Mock response.raise_for_status raises HTTPError
    - pytest.raises(requests.exceptions.HTTPError)

test_fetch_cash_rate_empty_csv(fixture_rba_response_empty)
    - Mock response returns empty CSV (header rows only, no data)
    - Verify empty DataFrame returned (dropna removes all)

test_fetch_cash_rate_value_range_extraction
    - Mock response with "17.00 to 17.50" value format
    - Verify regex extracts 17.50 (last number)

test_fetch_and_save_happy_path(fixture_rba_response)
    - Patch create_session + pipeline.config.DATA_DIR (via autouse fixture)
    - Verify returns row count > 0
    - Verify CSV file created at expected path
```

### Coverage target: 85%+ (45 statements, need ~39 covered)
- Lines 102-105 (__main__ block) excluded by convention

## Task 2: Create test_ingest_asx.py

Create `tests/python/test_ingest_asx.py` for asx_futures_scraper.py (144 statements).

### Patch targets:
- `pipeline.ingest.asx_futures_scraper.create_session`
- `pipeline.ingest.asx_futures_scraper.datetime` (for now() freezing)
- `builtins.open` or monkeypatch Path for meetings.json

### MockDatetime class:
```python
class MockDatetime(datetime):
    """datetime replacement that freezes now() but delegates everything else."""
    _fixed_now = datetime(2026, 2, 25, 10, 0, 0)

    @classmethod
    def now(cls, tz=None):
        return cls._fixed_now

    @classmethod
    def strptime(cls, date_string, format):
        return datetime.strptime(date_string, format)
```

### Test functions for _get_current_cash_rate:

```
test_get_current_cash_rate_from_csv
    - Write rba_cash_rate.csv to tmp_path (via isolate_data_dir)
    - Verify returns last row's value (e.g. 4.10)

test_get_current_cash_rate_missing_csv
    - Don't create CSV file
    - Verify returns fallback 4.35
```

### Test functions for _get_rba_meeting_dates:

```
test_get_rba_meeting_dates_happy_path
    - Create meetings.json at expected path (monkeypatch open or path)
    - Verify returns sorted list of date strings

test_get_rba_meeting_dates_missing_file
    - Don't create meetings.json
    - Verify returns empty list
```

### Test functions for _find_meeting_for_contract:

```
test_find_meeting_same_month
    - Contract 2026-03-31, meetings include 2026-03-18
    - Returns "2026-03-18"

test_find_meeting_nearest_future
    - Contract 2026-04-30, no April meeting, May meeting exists
    - Returns the May meeting date

test_find_meeting_no_suitable_meeting
    - Contract far in the future, no meetings after it
    - Returns None
```

### Test functions for _derive_probabilities:

```
test_derive_probabilities_implied_cut
    - implied_rate=3.60, current_rate=3.85 -> negative change_bp, probability_cut > 0

test_derive_probabilities_implied_hike
    - implied_rate=4.10, current_rate=3.85 -> positive change_bp, probability_hike > 0

test_derive_probabilities_hold_within_deadband
    - implied_rate=3.84, current_rate=3.85 -> within +/-5bp -> hold=100

test_derive_probabilities_probabilities_sum_to_100
    - Parametrize several rates, verify cut+hold+hike == 100

test_derive_probabilities_max_100_percent
    - Large implied cut (>25bp) -> probability_cut capped at 100
```

### Test functions for scrape_asx_futures:

```
test_scrape_asx_futures_happy_path(fixture_asx_response)
    - Mock session.get returns fixture JSON response
    - Mock _get_current_cash_rate, _get_rba_meeting_dates
    - Freeze datetime.now()
    - Verify returns DataFrame with expected columns
    - Verify 2 rows (matching fixture items)

test_scrape_asx_futures_empty_items(fixture_asx_response_empty)
    - Mock returns empty items
    - Verify returns empty DataFrame

test_scrape_asx_futures_http_error
    - Mock response.raise_for_status raises HTTPError
    - pytest.raises(requests.exceptions.HTTPError)

test_scrape_asx_futures_skips_invalid_rate
    - Mock response with settlement=200 (implied rate -100, out of 0-15 range)
    - Verify that item is skipped

test_scrape_asx_futures_skips_missing_expiry
    - Mock response with item missing dateExpiry
    - Verify skipped
```

### Test functions for _check_staleness:

```
test_check_staleness_fresh_data
    - Write CSV with today's date to tmp_path
    - Verify no error/warning logged (caplog)

test_check_staleness_14_day_warning
    - Write CSV with date 15 days ago
    - Verify warning logged

test_check_staleness_30_day_error
    - Write CSV with date 31 days ago
    - Verify error logged

test_check_staleness_missing_csv
    - Pass nonexistent path
    - Verify handles gracefully (warning logged)
```

### Test functions for fetch_and_save:

```
test_fetch_and_save_success
    - Patch scrape_asx_futures returning fixture DataFrame
    - Verify returns {'status': 'success', ...}
    - Verify CSV written

test_fetch_and_save_empty_scrape
    - Patch scrape_asx_futures returning empty DataFrame
    - Verify returns {'status': 'failed', ...}

test_fetch_and_save_exception
    - Patch scrape_asx_futures raising Exception
    - Verify returns {'status': 'failed', ...} (no raise)

test_fetch_and_save_deduplicates
    - Write existing CSV, then scrape returns overlapping rows
    - Verify deduplication on [date, meeting_date]
```

### Coverage target: 85%+ (144 statements, need ~123 covered)
- Lines 323-335 (__main__ block) excluded

## Task 3: Create test_ingest_corelogic.py

Create `tests/python/test_ingest_corelogic.py` for corelogic_scraper.py (114 statements).

### Patch targets:
- `pipeline.ingest.corelogic_scraper.create_session`
- `pipeline.ingest.corelogic_scraper.datetime` (for now() freezing)
- `pipeline.ingest.corelogic_scraper.pdfplumber` (mock pdfplumber.open)

### Test functions for get_candidate_urls:

```
test_get_candidate_urls_returns_4_urls
    - Call get_candidate_urls(2026, 2)
    - Verify returns list of 4 URLs
    - Verify URLs contain "Feb" or "February"

test_get_candidate_urls_december
    - Call get_candidate_urls(2025, 12)
    - Verify URLs contain "Dec" or "December"
```

### Test functions for download_cotality_pdf:

```
test_download_cotality_pdf_found
    - Mock session with first URL returning 200 + application/pdf content-type
    - Verify returns bytes

test_download_cotality_pdf_not_found
    - Mock session returning 404 for all candidates
    - Verify returns None

test_download_cotality_pdf_non_pdf_content_type
    - Mock session returning 200 but text/html content-type
    - Verify returns None (not treated as PDF)

test_download_cotality_pdf_request_exception
    - Mock session.get raises Exception
    - Verify returns None (continues to next candidate)
```

### Test functions for extract_cotality_yoy:

```
test_extract_cotality_yoy_happy_path
    - Mock pdfplumber.open context manager
    - Page.extract_text() returns "Australia 0.8% 2.4% 9.4%"
    - Verify returns 9.4

test_extract_cotality_yoy_pattern_not_found
    - Mock page text without "Australia X% X% X%" pattern
    - Verify returns None

test_extract_cotality_yoy_pdfplumber_import_error
    - Monkeypatch pdfplumber to raise ImportError
    - Verify returns None

test_extract_cotality_yoy_negative_values
    - Mock page text "Australia -0.3% -1.2% -2.5%"
    - Verify returns -2.5
```

### Test functions for _current_month_already_scraped:

```
test_current_month_already_scraped_true
    - Write CSV with current month date and source='Cotality HVI'
    - Freeze datetime.now()
    - Verify returns True

test_current_month_already_scraped_false_old_data
    - Write CSV with last month's date
    - Verify returns False

test_current_month_already_scraped_false_no_file
    - Verify returns False when file doesn't exist

test_current_month_already_scraped_false_empty_csv
    - Write empty CSV
    - Verify returns False
```

### Test functions for scrape_cotality:

```
test_scrape_cotality_happy_path
    - Mock session, datetime, pdfplumber
    - Verify returns single-row DataFrame with expected columns

test_scrape_cotality_already_scraped
    - Write CSV with current month data
    - Verify returns empty DataFrame

test_scrape_cotality_no_pdf_found
    - Mock session returning 404 for all candidate URLs
    - Verify returns empty DataFrame

test_scrape_cotality_tries_previous_month
    - Mock current month PDF not found, previous month found
    - Verify extracts from previous month's PDF

test_scrape_cotality_december_period_end
    - Freeze datetime to January, test previous month = December
    - Verify period_end calculation handles December correctly (year+1)
```

### Test functions for fetch_and_save:

```
test_fetch_and_save_success
    - Patch scrape_cotality returning fixture DataFrame
    - Verify returns {'status': 'success', ...}

test_fetch_and_save_empty_scrape
    - Patch scrape_cotality returning empty DataFrame
    - Verify returns {'status': 'failed', ...}

test_fetch_and_save_exception
    - Patch scrape_cotality raising Exception
    - Verify returns {'status': 'failed', ...} (never raises)
```

### Coverage target: 85%+ (114 statements, need ~97 covered)
- Lines 260-264 (__main__ block) excluded

## Task 4: Create test_ingest_nab.py

Create `tests/python/test_ingest_nab.py` for nab_scraper.py (203 statements).

### Patch targets:
- `pipeline.ingest.nab_scraper.create_session`
- `pipeline.ingest.nab_scraper.datetime` (for now() freezing)
- `pipeline.ingest.nab_scraper.pdfplumber` (for PDF fallback)

### Test functions for discover_latest_survey_url:

```
test_discover_latest_survey_url_found(fixture_nab_html)
    - Mock session: first tag archive returns HTML with survey link
    - Verify returns absolute URL containing "monthly-business-survey"

test_discover_latest_survey_url_relative_href
    - Mock HTML with relative href "/nab-monthly-business-survey-jan-2026"
    - Verify returns NAB_BASE + relative path

test_discover_latest_survey_url_not_found
    - Mock session: all archives return HTML without survey links
    - Verify returns None

test_discover_latest_survey_url_archive_error
    - Mock session: all archives raise Exception
    - Verify returns None (handles gracefully)

test_discover_latest_survey_url_non_200
    - Mock session: archives return 404
    - Verify returns None
```

### Test functions for fetch_article:

```
test_fetch_article_success
    - Mock session.get returns 200 with content
    - Verify returns bytes

test_fetch_article_non_200
    - Mock session.get returns 404
    - Verify returns None

test_fetch_article_exception
    - Mock session.get raises Exception
    - Verify returns None
```

### Test functions for extract_capacity_from_html:

```
test_extract_capacity_from_html_found(fixture_nab_html)
    - Pass fixture HTML bytes
    - Verify returns 83.6

test_extract_capacity_from_html_not_found(fixture_nab_html_no_data)
    - Pass fixture HTML without capacity data
    - Verify returns None

test_extract_capacity_from_html_us_spelling
    - Create HTML with "capacity utilization" (z spelling)
    - Verify regex still matches
```

### Test functions for get_pdf_link:

```
test_get_pdf_link_found(fixture_nab_html)
    - Pass fixture HTML bytes with PDF link
    - Verify returns URL ending in .pdf

test_get_pdf_link_not_found(fixture_nab_html_no_data)
    - Pass fixture HTML without PDF link
    - Verify returns None

test_get_pdf_link_relative_url
    - Create HTML with relative PDF href
    - Verify returns NAB_BASE + relative path
```

### Test functions for extract_capacity_from_pdf:

```
test_extract_capacity_from_pdf_found
    - Mock pdfplumber.open context manager
    - Page text: "Capacity utilisation 83.2%"
    - Verify returns 83.2

test_extract_capacity_from_pdf_not_found
    - Mock page text without capacity pattern
    - Verify returns None

test_extract_capacity_from_pdf_import_error
    - Monkeypatch pdfplumber to raise ImportError
    - Verify returns None

test_extract_capacity_from_pdf_exception
    - Mock pdfplumber.open raising Exception
    - Verify returns None
```

### Test functions for _current_month_already_scraped:

```
test_current_month_already_scraped_nab_true
    - Write CSV with current month date
    - Freeze datetime.now()
    - Verify returns True

test_current_month_already_scraped_nab_false
    - Write CSV with old date
    - Verify returns False

test_current_month_already_scraped_nab_no_file
    - Verify returns False
```

### Test functions for backfill_nab_history:

```
test_backfill_nab_history_success
    - Mock session: 2 of 3 months return article HTML with capacity data
    - Call backfill_nab_history(session, months=2)
    - Verify returns count of successful scrapes
    - Verify CSV has rows appended

test_backfill_nab_history_pdf_fallback
    - Mock session: HTML has no capacity data, but has PDF link
    - Mock PDF response + pdfplumber extraction
    - Verify PDF fallback path exercised

test_backfill_nab_history_all_fail
    - Mock session: all URLs return 404
    - Verify returns 0
```

### Test functions for scrape_nab_capacity:

```
test_scrape_nab_capacity_happy_path
    - Mock session, datetime
    - Discover URL -> fetch article -> extract HTML -> return DataFrame
    - Verify returns single-row DataFrame

test_scrape_nab_capacity_already_scraped
    - Write current month CSV
    - Verify returns empty DataFrame

test_scrape_nab_capacity_no_url_discovered
    - Mock discover_latest_survey_url returns None
    - Verify returns empty DataFrame

test_scrape_nab_capacity_html_fails_pdf_succeeds
    - Mock HTML extraction None, PDF extraction succeeds
    - Verify PDF fallback used

test_scrape_nab_capacity_both_fail
    - Mock both HTML and PDF extraction return None
    - Verify returns empty DataFrame

test_scrape_nab_capacity_triggers_backfill_no_csv
    - Don't create CSV file
    - Verify backfill_nab_history called

test_scrape_nab_capacity_triggers_backfill_sparse_csv
    - Write CSV with < 3 rows
    - Verify backfill_nab_history called

test_scrape_nab_capacity_no_pdf_link
    - Mock HTML extraction None, no PDF link in HTML
    - Verify returns empty DataFrame (specific path through code)
```

### Test functions for fetch_and_save:

```
test_nab_fetch_and_save_success
    - Patch scrape_nab_capacity returning fixture DataFrame
    - Verify returns {'status': 'success', ...}

test_nab_fetch_and_save_empty_with_backfill_data
    - Patch scrape_nab_capacity returning empty, but CSV exists with data
    - Verify returns {'status': 'success', ...} (backfill data counts)

test_nab_fetch_and_save_empty_no_data
    - Patch scrape_nab_capacity returning empty, no CSV exists
    - Verify returns {'status': 'failed', ...}

test_nab_fetch_and_save_exception
    - Patch scrape_nab_capacity raising Exception
    - Verify returns {'status': 'failed', ...} (never raises)
```

### Coverage target: 85%+ (203 statements, need ~173 covered)
- Lines 381-386 (__main__ block) excluded

## Verification

After all 4 test files are created:

1. `pytest tests/python/test_ingest_rba.py -v` — all pass
2. `pytest tests/python/test_ingest_asx.py -v` — all pass
3. `pytest tests/python/test_ingest_corelogic.py -v` — all pass
4. `pytest tests/python/test_ingest_nab.py -v` — all pass
5. `pytest tests/python/ -m "not live" -q` — all tests pass (existing + new)
6. Coverage check: all 4 modules >= 85%
7. No test makes real HTTP requests (socket blocker active)
8. Date-dependent tests produce consistent results (datetime frozen)

---
*Plan 19-02 created: 2026-02-25*
