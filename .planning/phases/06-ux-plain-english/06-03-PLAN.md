---
phase: 06-ux-plain-english
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - public/index.html
  - public/js/gauge-init.js
  - public/js/interpretations.js
autonomous: true

must_haves:
  truths:
    - "Meta title says 'Are Australian interest rates going up or down?' and description mentions 'Free tool'"
    - "Missing indicators show greyed-out placeholder cards with 'Data coming soon' and weight percentage"
    - "ASX Futures panel is completely hidden when data is null/missing (no heading, no 'unavailable' message)"
    - "A contextual paragraph above the calculator connects the hawk score to mortgage impact"
    - "ASX Futures heading reads 'What Markets Expect' with subline about ASX 30 Day Interbank Cash Rate Futures"
    - "A 'See what this means for your mortgage' anchor link appears in the hero section and smooth-scrolls to #calculator-section"
    - "A 'What to do next' section appears before the footer with 3 actionable items"
    - "Empty ASX panel hidden on mobile; rate history chart collapsible on small screens"
  artifacts:
    - path: "public/index.html"
      provides: "Updated meta tags, jump-to-calculator link, what-to-do-next section, chart details/summary wrapper"
    - path: "public/js/gauge-init.js"
      provides: "Calculator bridge paragraph rendering, placeholder card rendering for missing indicators"
    - path: "public/js/interpretations.js"
      provides: "Hidden ASX container when data null, retitled ASX heading"
  key_links:
    - from: "public/js/gauge-init.js"
      to: "status.json metadata.indicators_missing"
      via: "forEach loop rendering placeholder cards after metric gauges"
      pattern: "indicators_missing"
    - from: "public/js/gauge-init.js"
      to: "#calculator-section"
      via: "dynamic bridge paragraph inserted before calculator"
      pattern: "calculator-bridge"
    - from: "public/js/interpretations.js"
      to: "#asx-futures-container"
      via: "display:none when asxData is null"
      pattern: "display.*none"
---

<objective>
Improve information architecture, connect the hawk score to the mortgage calculator, handle missing/empty UI states gracefully, and polish mobile experience.

Purpose: Users currently see a disconnected page with empty panels, buried calculator, and no guidance on what to do with the information. This plan bridges the analytical gauges to the personal-impact calculator, shows honest placeholders for missing data, hides empty panels, and adds mobile-friendly collapsibility.

Output: Updated index.html with meta tags, anchor link, chart toggle, and CTA section. Updated gauge-init.js with calculator bridge text and placeholder cards. Updated interpretations.js with hidden empty ASX panel and retitled heading.
</objective>

<execution_context>
@/Users/annon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/annon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ux-plain-english/06-CONTEXT.md
@.planning/phases/06-ux-plain-english/06-RESEARCH.md
@public/index.html
@public/js/gauge-init.js
@public/js/interpretations.js
@public/data/status.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Meta tags, jump-to-calculator link, and ASX heading retitle</name>
  <files>public/index.html, public/js/interpretations.js</files>
  <action>
  **index.html changes:**

  1. Update meta title (line 6) to:
     `RBA Hawk-O-Meter | Are Australian interest rates going up or down?`

  2. Update meta description (line 7) to:
     `Free tool tracking economic data to show whether Australian interest rates are likely to rise, fall, or stay the same. Updated automatically.`

  3. Add a "Jump to calculator" anchor link inside the `#hawk-o-meter-section`, immediately after `<div id="verdict-container">` (after line 101). Create a `<div>` with id `calculator-jump-link` that will be populated by JS in gauge-init.js (Task 3). For the static fallback, add:
     ```
     <div id="calculator-jump-link" class="mt-3 text-center"></div>
     ```
     This goes between the verdict container close (line 101) and the section close (line 102).

  4. Change the static ASX heading text in the HTML (line 85). Replace:
     `ASX Futures &mdash; Next Meeting`
     with:
     `What Markets Expect`
     (The JS will also be updated to match.)

  **interpretations.js changes:**

  5. In `renderASXTable()` (line 60-146), make two changes:

     a. When `asxData` is null/missing (the `if (!asxData || !asxData.probabilities)` branch at line 70), instead of rendering a heading + "unavailable" paragraph, hide the entire container:
        ```js
        if (!asxData || !asxData.probabilities) {
          container.style.display = 'none';
          return;
        }
        ```
        Remove the existing code that creates the heading + unavail paragraph in the null branch.

     b. When data IS available, ensure the container is visible (`container.style.display = ''`) and update the heading text (line 67) from `'ASX Futures \u2014 Next Meeting'` to `'What Markets Expect'`. After the heading, add a subline paragraph:
        ```js
        var subline = document.createElement('p');
        subline.className = 'text-xs text-gray-500 mb-3';
        subline.textContent = 'Based on ASX 30 Day Interbank Cash Rate Futures pricing';
        container.appendChild(subline);
        ```

  Commit: `feat(06-03): update meta tags, ASX heading, hide empty ASX panel, add calculator jump point`
  </action>
  <verify>
  - Open index.html in browser, check page title reads "RBA Hawk-O-Meter | Are Australian interest rates going up or down?"
  - View page source, confirm meta description updated
  - ASX Futures container should be completely hidden (no heading, no "unavailable" text) since status.json has no asx_futures data
  - A `#calculator-jump-link` div exists after the verdict (empty for now, populated in Task 3)
  </verify>
  <done>
  Meta title and description updated. ASX panel hidden when data unavailable. ASX heading retitled to "What Markets Expect" with subline. Calculator jump-link placeholder in DOM.
  </done>
</task>

<task type="auto">
  <name>Task 2: Placeholder cards for missing indicators</name>
  <files>public/js/gauge-init.js</files>
  <action>
  In `gauge-init.js`, after the `renderMetricGauges()` function renders available metrics (after the `orderedIds.forEach` loop ending ~line 72), add logic to render greyed-out placeholder cards for missing indicators.

  1. Add a new function `renderMissingIndicatorCards(container, missingList, weightsObj)`:
     - Takes the `metric-gauges-grid` container, `metadata.indicators_missing` array, and `data.weights` object
     - For each indicator in `missingList` (skip `asx_futures` since it's a benchmark, not an indicator card):
       - Create a card div: `bg-finance-gray/50 rounded-lg p-4 border border-finance-border/50 opacity-60`
       - Header row (flex, items-center, justify-between, mb-2):
         - h4 with class `font-semibold text-gray-500`: use `GaugesModule.getDisplayLabel(indicatorId)` for display name
         - Weight badge span: `text-xs text-gray-600`, text = `Math.round(weight * 100) + '% weight'`
       - Placeholder text paragraph: `text-sm text-gray-600 italic`, text = `'Data coming soon'`
       - Append card to container

  2. In `initGauges()`, after the `renderMetricGauges(data.gauges)` call (~line 96), add:
     ```js
     // Render placeholder cards for missing indicators
     if (data.metadata && Array.isArray(data.metadata.indicators_missing)) {
       var gridContainer = document.getElementById('metric-gauges-grid');
       if (gridContainer) {
         renderMissingIndicatorCards(gridContainer, data.metadata.indicators_missing, data.weights || {});
       }
     }
     ```

  This will render greyed-out cards for employment, spending, housing, business_confidence (but NOT asx_futures) after the 3 active metric cards.

  Commit: `feat(06-03): add placeholder cards for missing indicators`
  </action>
  <verify>
  - Open page in browser, scroll to "Economic Indicators" section
  - Should see 3 active metric cards (inflation, wages, building_approvals) followed by 4 greyed-out placeholder cards (Jobs, Spending, Housing, Capacity)
  - Each placeholder shows indicator name, weight percentage, and "Data coming soon" in italic
  - asx_futures should NOT appear as a placeholder card
  </verify>
  <done>
  Missing indicators display as greyed-out placeholder cards with name, weight, and "Data coming soon" text. ASX futures excluded from placeholders.
  </done>
</task>

<task type="auto">
  <name>Task 3: Calculator bridge text, jump link, and "What to do next" section</name>
  <files>public/js/gauge-init.js, public/index.html</files>
  <action>
  **gauge-init.js changes:**

  1. In `initGauges()`, after rendering the verdict (~line 87), populate the calculator jump link. Add:
     ```js
     // Render "jump to calculator" link in hero
     var jumpContainer = document.getElementById('calculator-jump-link');
     if (jumpContainer) {
       var jumpLink = document.createElement('a');
       jumpLink.href = '#calculator-section';
       jumpLink.className = 'inline-block text-sm text-finance-accent hover:underline mt-1';
       jumpLink.textContent = 'See what this means for your mortgage \u2193';
       jumpContainer.appendChild(jumpLink);
     }
     ```

  2. After rendering metric gauges and placeholder cards, render a calculator bridge paragraph. Add a new function `renderCalculatorBridge(score, zoneLabel)`:
     - Find or create a `<div id="calculator-bridge">` element. Insert it before the calculator section's existing `<h2>` heading. Implementation: find the `#calculator-section` element, create the bridge div, and insert it as the first child of that section (before the existing h2).
     - The bridge div: class `mb-4 p-4 bg-finance-gray border border-finance-border rounded-lg`
     - Create a paragraph with class `text-sm text-gray-300`:
       - Text: `'The Hawk-O-Meter reads ' + Math.round(score) + '/100 (' + label + '). Here\u2019s what current and potential rate changes could mean for a typical mortgage.'`
       - Where `label` is a lowercased plain-English version of the zone label. Use `GaugesModule.getStanceLabel(score).toLowerCase()` to get e.g. "neutral", "dovish", "hawkish".
     - Note: Use neutral language per ASIC RG 244 -- "could mean" not "will mean" or "means".

  3. Call `renderCalculatorBridge(data.overall.hawk_score, data.overall.zone_label)` inside the `.then()` callback in `initGauges()`, after rendering metric gauges.

  **index.html changes:**

  4. Add a "What to do next" section before the footer. Insert between `</main>` (line 267) and `<footer>` (line 270). Create a `<section>` with appropriate styling:
     ```html
     <!-- What to do next -->
     <section aria-label="What to do next" class="max-w-6xl mx-auto w-full px-4 sm:px-8 py-8">
       <h2 class="text-lg font-semibold text-gray-200 mb-4">What to Do Next</h2>
       <div class="grid grid-cols-1 sm:grid-cols-3 gap-4">
         <div class="bg-finance-gray rounded-lg p-4 border border-finance-border">
           <p class="text-sm font-medium text-gray-300 mb-1">Bookmark this page</p>
           <p class="text-xs text-gray-500">The data updates automatically. Check back anytime to see the latest score.</p>
         </div>
         <div class="bg-finance-gray rounded-lg p-4 border border-finance-border">
           <p class="text-sm font-medium text-gray-300 mb-1">Check back after the next RBA meeting</p>
           <p class="text-xs text-gray-500">The Reserve Bank meets roughly every 6 weeks to decide on interest rates.</p>
         </div>
         <div class="bg-finance-gray rounded-lg p-4 border border-finance-border">
           <p class="text-sm font-medium text-gray-300 mb-1">Speak to a licensed financial adviser</p>
           <p class="text-xs text-gray-500">This tool shows data, not advice. A licensed adviser can help with decisions specific to your situation.</p>
         </div>
       </div>
     </section>
     ```

  Note: This section sits outside `<main>` but before `<footer>`, matching the visual separation pattern. It uses ASIC-compliant language ("data, not advice", "licensed adviser").

  Commit: `feat(06-03): add calculator bridge text, jump link, and what-to-do-next section`
  </action>
  <verify>
  - Open page in browser
  - Hero section: after the verdict text, a small blue link reads "See what this means for your mortgage ↓". Clicking it smooth-scrolls to the calculator.
  - Above the calculator form: a contextual paragraph reads "The Hawk-O-Meter reads 46/100 (neutral). Here's what current and potential rate changes could mean for a typical mortgage."
  - Before the footer: "What to Do Next" section shows 3 cards (bookmark, check back, speak to adviser)
  - All language is neutral/ASIC-compliant (no financial advice)
  </verify>
  <done>
  Calculator bridge connects hawk score to mortgage calculator with dynamic text. Jump link in hero smooth-scrolls to calculator. "What to Do Next" section provides 3 actionable items before footer.
  </done>
</task>

<task type="auto">
  <name>Task 4: Mobile UX improvements — hide empty ASX, collapsible chart</name>
  <files>public/index.html</files>
  <action>
  1. **Collapsible chart on mobile**: Wrap the rate chart section content inside a `<details>/<summary>` element on small screens. The `<details>` approach provides native accessibility and requires no JS.

     In `index.html`, replace the chart section (lines 110-118) with:
     ```html
     <section aria-label="Historical rate chart">
       <details id="chart-details" class="group">
         <summary class="flex items-center justify-between cursor-pointer list-none">
           <h2 class="text-lg font-semibold text-gray-200">Cash Rate History</h2>
           <span class="text-sm text-gray-500 group-open:hidden sm:hidden">Show chart</span>
           <span class="text-sm text-gray-500 hidden group-open:inline sm:hidden">Hide chart</span>
         </summary>
         <div id="rate-chart" class="w-full h-64 sm:h-80 md:h-96 lg:h-[500px] bg-finance-gray border border-finance-border rounded-xl overflow-hidden mt-4">
           <div class="flex items-center justify-center h-full">
             <p class="text-gray-500 text-sm">Loading chart data...</p>
           </div>
         </div>
         <p id="chart-source" class="text-xs text-gray-600 mt-2">Source: Reserve Bank of Australia</p>
       </details>
     </section>
     ```

     Add a `<style>` block inside the existing `<style>` tag (before line 44) for the `<details>` element:
     ```css
     /* Chart details: open by default on sm+ screens */
     @media (min-width: 640px) {
       #chart-details { pointer-events: none; }
       #chart-details > summary { pointer-events: none; list-style: none; }
       #chart-details > summary span { display: none !important; }
       #chart-details > div, #chart-details > p { display: block !important; }
     }
     details summary::-webkit-details-marker { display: none; }
     ```

     Also add an inline `open` attribute so the chart is visible by default: `<details id="chart-details" open class="group">`. On mobile, users can collapse it. On desktop, the pointer-events:none CSS prevents toggling (always open).

  2. **ASX panel already handled**: Task 1 already hides the ASX container entirely when data is null via `container.style.display = 'none'` in interpretations.js. This covers both mobile and desktop. No additional mobile-specific hiding needed.

  Commit: `feat(06-03): collapsible chart on mobile, finalize mobile UX`
  </action>
  <verify>
  - Open page on mobile viewport (< 640px): chart section shows "Cash Rate History" heading with "Show chart" toggle text. Tapping toggles chart visibility.
  - On desktop (>= 640px): chart is always visible, toggle text hidden, not interactive.
  - ASX panel is hidden on all viewports when data is null (already verified in Task 1).
  - Verify Plotly chart still renders correctly inside the `<details>` element when open.
  </verify>
  <done>
  Rate history chart is collapsible on mobile via native details/summary. Desktop always shows chart. Empty ASX panel hidden on all viewports.
  </done>
</task>

</tasks>

<verification>
1. `grep -c 'Are Australian interest rates going up or down' public/index.html` returns 1
2. `grep -c 'Free tool tracking' public/index.html` returns 1
3. `grep -c 'display.*none' public/js/interpretations.js` confirms ASX hide logic exists
4. `grep -c 'What Markets Expect' public/js/interpretations.js` returns 1
5. `grep -c 'indicators_missing' public/js/gauge-init.js` confirms placeholder card logic exists
6. `grep -c 'calculator-bridge' public/js/gauge-init.js` confirms bridge paragraph logic exists
7. `grep -c 'calculator-jump-link' public/index.html` returns 1
8. `grep -c 'What to Do Next' public/index.html` returns 1
9. `grep -c 'chart-details' public/index.html` returns at least 2 (details element + CSS)
10. Open page in browser at mobile width: chart is collapsible, ASX hidden, placeholders visible
</verification>

<success_criteria>
- Page meta tags updated for SEO and human clarity
- Empty ASX Futures panel completely hidden (no residual heading or "unavailable" text)
- ASX heading reads "What Markets Expect" when data is present
- 4 greyed-out placeholder cards for missing indicators (employment, spending, housing, business_confidence) with weights and "Data coming soon"
- Calculator bridge paragraph dynamically shows current score and zone with neutral language
- "See what this means for your mortgage" link in hero smooth-scrolls to calculator
- "What to Do Next" section with 3 actionable cards before footer
- Rate history chart collapsible on mobile, always visible on desktop
- All text ASIC RG 244 compliant (no financial advice language)
- All DOM manipulation uses safe methods (createElement/textContent, no innerHTML)
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-plain-english/06-03-SUMMARY.md`
</output>
