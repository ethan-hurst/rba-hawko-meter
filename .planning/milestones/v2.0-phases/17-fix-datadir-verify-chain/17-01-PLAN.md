---
phase: 17-fix-datadir-verify-chain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pipeline/config.py
  - pipeline/ingest/abs_data.py
  - pipeline/ingest/rba_data.py
  - pipeline/ingest/asx_futures_scraper.py
  - pipeline/ingest/corelogic_scraper.py
  - pipeline/ingest/nab_scraper.py
  - pipeline/normalize/ratios.py
  - pipeline/normalize/engine.py
autonomous: true
requirements:
  - LIVE-01
  - LIVE-02

must_haves:
  truths:
    - "All 5 ingestors read pipeline.config.DATA_DIR at call time, not import time"
    - "normalize/ratios.py and normalize/engine.py read pipeline.config.DATA_DIR at call time"
    - "DATA_DIR environment variable override takes precedence over default"
    - "monkeypatch.setattr(pipeline.config, 'DATA_DIR', tmp_path) is visible to all ingestor and normalize code"
  artifacts:
    - path: "pipeline/config.py"
      provides: "Environment variable override for DATA_DIR"
      contains: "os.environ.get"
    - path: "pipeline/ingest/abs_data.py"
      provides: "Late-bound DATA_DIR access"
      contains: "pipeline.config.DATA_DIR"
    - path: "pipeline/ingest/rba_data.py"
      provides: "Late-bound DATA_DIR access"
      contains: "pipeline.config.DATA_DIR"
    - path: "pipeline/ingest/asx_futures_scraper.py"
      provides: "Late-bound DATA_DIR access"
      contains: "pipeline.config.DATA_DIR"
    - path: "pipeline/ingest/corelogic_scraper.py"
      provides: "Late-bound DATA_DIR access"
      contains: "pipeline.config.DATA_DIR"
    - path: "pipeline/ingest/nab_scraper.py"
      provides: "Late-bound DATA_DIR access"
      contains: "pipeline.config.DATA_DIR"
    - path: "pipeline/normalize/ratios.py"
      provides: "Late-bound DATA_DIR access"
      contains: "pipeline.config.DATA_DIR"
    - path: "pipeline/normalize/engine.py"
      provides: "Late-bound DATA_DIR access"
      contains: "pipeline.config.DATA_DIR"
  key_links:
    - from: "tests/python/conftest.py"
      to: "pipeline/config.py"
      via: "monkeypatch.setattr(pipeline.config, 'DATA_DIR', tmp_path)"
      pattern: "monkeypatch\\.setattr.*DATA_DIR"
    - from: "pipeline/ingest/abs_data.py"
      to: "pipeline/config.py"
      via: "pipeline.config.DATA_DIR attribute access at call time"
      pattern: "pipeline\\.config\\.DATA_DIR"
---

<objective>
Fix import-time DATA_DIR binding across all 7 affected Python modules so that monkeypatching pipeline.config.DATA_DIR in tests is visible to all code at call time. Add environment variable override support.

Purpose: Close the root cause of the DATA_DIR wiring bug identified in the v2.0 milestone audit. Without this fix, live tests write to the real data/ directory while asserting against tmp_path, causing AssertionError.
Output: 8 modified Python files with late-bound DATA_DIR access pattern.
</objective>

<execution_context>
@/Users/annon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/annon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/17-fix-datadir-verify-chain/17-RESEARCH.md
@.planning/phases/17-fix-datadir-verify-chain/17-CONTEXT.md
@pipeline/config.py
@pipeline/ingest/abs_data.py
@pipeline/ingest/rba_data.py
@pipeline/ingest/asx_futures_scraper.py
@pipeline/ingest/corelogic_scraper.py
@pipeline/ingest/nab_scraper.py
@pipeline/normalize/ratios.py
@pipeline/normalize/engine.py
@tests/python/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add environment variable override to config.py and fix DATA_DIR binding in all 7 modules</name>
  <files>
    pipeline/config.py
    pipeline/ingest/abs_data.py
    pipeline/ingest/rba_data.py
    pipeline/ingest/asx_futures_scraper.py
    pipeline/ingest/corelogic_scraper.py
    pipeline/ingest/nab_scraper.py
    pipeline/normalize/ratios.py
    pipeline/normalize/engine.py
  </files>
  <action>
**Step 1: Update pipeline/config.py** to support DATA_DIR environment variable override:
- Add `import os` at the top
- Change `DATA_DIR = Path("data")` to `DATA_DIR = Path(os.environ.get("DATA_DIR", "data"))`
- Leave SOURCE_METADATA and WEIGHTS_FILE and STATUS_OUTPUT unchanged (they use import-time DATA_DIR which is acceptable for production; tests that need them already know about this limitation per conftest.py lines 42-45)

**Step 2: Fix all 5 ingestors** — for each file, apply this pattern:
1. Remove `DATA_DIR` from the `from pipeline.config import ...` line
2. Add `import pipeline.config` if not already present
3. Replace every bare `DATA_DIR` reference in function bodies with `pipeline.config.DATA_DIR`
4. Do NOT change imports of other config values (ABS_API_BASE, ABS_CONFIG, DEFAULT_TIMEOUT, etc.) — only DATA_DIR needs late binding

Specific files and their DATA_DIR usage locations:

**pipeline/ingest/abs_data.py:**
- Line 14: Remove `DATA_DIR` from import tuple
- Add `import pipeline.config` after existing imports
- Line 296: `DATA_DIR / output_file` → `pipeline.config.DATA_DIR / output_file`
- Line 305: Same pattern

**pipeline/ingest/rba_data.py:**
- Line 10: Remove `DATA_DIR` from import, keep `DEFAULT_TIMEOUT, RBA_BASE_URL, RBA_CONFIG`
- Add `import pipeline.config`
- Line 91: `DATA_DIR / RBA_CONFIG[...]` → `pipeline.config.DATA_DIR / RBA_CONFIG[...]`

**pipeline/ingest/asx_futures_scraper.py:**
- Line 25: Remove `DATA_DIR` from import tuple
- Add `import pipeline.config`
- Line 42: `DATA_DIR / "rba_cash_rate.csv"` → `pipeline.config.DATA_DIR / "rba_cash_rate.csv"`
- Line 278: `DATA_DIR / "asx_futures.csv"` → `pipeline.config.DATA_DIR / "asx_futures.csv"`
- Line 330: `DATA_DIR / "asx_futures.csv"` → `pipeline.config.DATA_DIR / "asx_futures.csv"`

**pipeline/ingest/corelogic_scraper.py:**
- Line 18: Remove `DATA_DIR` from import, keep `BROWSER_USER_AGENT, DEFAULT_TIMEOUT`
- Add `import pipeline.config`
- Line 167: `DATA_DIR / OUTPUT_FILE` → `pipeline.config.DATA_DIR / OUTPUT_FILE`
- Line 240: Same pattern

**pipeline/ingest/nab_scraper.py:**
- Line 20: Remove `DATA_DIR` from import, keep `BROWSER_USER_AGENT, DEFAULT_TIMEOUT`
- Add `import pipeline.config`
- Lines 190, 270, 361, 370: All `DATA_DIR / OUTPUT_FILE` → `pipeline.config.DATA_DIR / OUTPUT_FILE`

**Step 3: Fix 2 normalize modules:**

**pipeline/normalize/ratios.py:**
- Line 13: Remove `from pipeline.config import DATA_DIR`
- Add `import pipeline.config`
- Line 125: `DATA_DIR / csv_file` → `pipeline.config.DATA_DIR / csv_file`

**pipeline/normalize/engine.py:**
- Line 16: Remove `DATA_DIR` from the `from pipeline.config import (...)` block, keep all other imports (INDICATOR_CONFIG, OPTIONAL_INDICATOR_CONFIG, STATUS_OUTPUT, WEIGHTS_FILE, ZSCORE_*)
- Add `import pipeline.config` at top of import block (if not already — check first)
- Line 152: `DATA_DIR / "corelogic_housing.csv"` → `pipeline.config.DATA_DIR / "corelogic_housing.csv"`
- Line 188: This line already does `from pipeline.config import DATA_DIR as _DATA_DIR` inside a function — change to `_DATA_DIR = pipeline.config.DATA_DIR`
- Line 228: `DATA_DIR / "asx_futures.csv"` → `pipeline.config.DATA_DIR / "asx_futures.csv"`

**Step 4: Add startup logging to each ingestor's main entry function.**
Add a single log line at the top of each `fetch_and_save()` function:
```python
logger.info("DATA_DIR: %s", pipeline.config.DATA_DIR)
```
For abs_data.py, add a logger if one doesn't exist:
```python
import logging
logger = logging.getLogger(__name__)
```
For rba_data.py, same pattern — add logger if missing.
Note: asx_futures_scraper.py, corelogic_scraper.py, and nab_scraper.py already have loggers.

**Step 5: Verify no remaining import-time DATA_DIR bindings:**
Run: `grep -rn "from pipeline.config import.*DATA_DIR" pipeline/`
The ONLY match should be in config.py itself (if any self-reference) or in files like SOURCE_METADATA that are acceptable. Zero matches in ingest/ or normalize/ directories.
  </action>
  <verify>
    <automated>cd /Users/annon/projects/rba-hawko-meter && grep -rn "from pipeline.config import.*DATA_DIR" pipeline/ingest/ pipeline/normalize/ | grep -v "^$" | wc -l | tr -d ' '</automated>
    <manual>Output should be "0" — no import-time DATA_DIR bindings remain in ingest/ or normalize/. Also run: python -c "import pipeline.config; print(pipeline.config.DATA_DIR)" to confirm module loads without error.</manual>
  </verify>
  <done>
    - Zero occurrences of `from pipeline.config import ... DATA_DIR` in pipeline/ingest/ and pipeline/normalize/
    - All 7 modules use `pipeline.config.DATA_DIR` at call time
    - config.py supports DATA_DIR environment variable override
    - Each ingestor's fetch_and_save() logs DATA_DIR at startup
    - Existing unit tests still pass: `pytest tests/python/ -m "not live"` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify unit tests pass with late-bound DATA_DIR</name>
  <files></files>
  <action>
Run the full non-live test suite to confirm the import changes don't break anything:

```bash
cd /Users/annon/projects/rba-hawko-meter && python -m pytest tests/python/ -m "not live" -v
```

All existing tests must pass. The `isolate_data_dir` autouse fixture already patches `pipeline.config.DATA_DIR` — with the late-binding fix, any test that exercises ingestor or normalize code paths will now correctly use the tmp_path.

If any test fails:
1. Check if the failure is related to the import change (e.g., NameError for DATA_DIR)
2. Fix the specific module where the reference was missed
3. Re-run tests

Also verify ruff linting passes on all modified files:
```bash
ruff check pipeline/config.py pipeline/ingest/ pipeline/normalize/ratios.py pipeline/normalize/engine.py
```
Fix any linting issues (e.g., unused imports if DATA_DIR was the only import from a line, import ordering).
  </action>
  <verify>
    <automated>cd /Users/annon/projects/rba-hawko-meter && python -m pytest tests/python/ -m "not live" -q && ruff check pipeline/config.py pipeline/ingest/ pipeline/normalize/ratios.py pipeline/normalize/engine.py</automated>
  </verify>
  <done>
    - All non-live pytest tests pass (exit 0)
    - Ruff reports 0 violations on all modified files
    - No regressions introduced by the import pattern change
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "from pipeline.config import.*DATA_DIR" pipeline/ingest/ pipeline/normalize/` returns empty (zero import-time bindings)
2. `python -m pytest tests/python/ -m "not live" -q` passes all tests
3. `ruff check pipeline/` reports 0 violations
4. `python -c "import os; os.environ['DATA_DIR']='/tmp/test'; import pipeline.config; assert str(pipeline.config.DATA_DIR) == '/tmp/test'"` succeeds (env var override works)
</verification>

<success_criteria>
- All 7 modules use late-bound pipeline.config.DATA_DIR
- Environment variable DATA_DIR override works
- Unit tests pass without regressions
- Ruff linting clean on all modified files
</success_criteria>

<output>
After completion, create `.planning/phases/17-fix-datadir-verify-chain/17-01-SUMMARY.md`
</output>
