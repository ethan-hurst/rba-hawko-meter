---
phase: 16-verify-linting-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13-linting-baseline/13-VERIFICATION.md
autonomous: true
gap_closure: true
requirements: [LINT-01, LINT-02, LINT-03, LINT-04]

must_haves:
  truths:
    - "`npm run lint:py` exits 0 with 'All checks passed!' output"
    - "`npm run lint:js` exits 0 with no violations"
    - "`npm run lint` runs both linters in sequence and both exit 0"
    - "Introducing an unused import in a pipeline file causes `npm run lint:py` to fail with a violation pointing to the file and line"
  artifacts:
    - path: ".planning/phases/13-linting-baseline/13-VERIFICATION.md"
      provides: "Phase 13 verification report confirming all 4 LINT requirements satisfied"
      contains: "status: passed"
  key_links:
    - from: "pyproject.toml [tool.ruff]"
      to: "npm run lint:py"
      via: "ruff check pipeline/ tests/"
      pattern: "ruff check pipeline/ tests/"
    - from: "eslint.config.js"
      to: "npm run lint:js"
      via: "npx eslint public/js/"
      pattern: "npx eslint public/js/"
    - from: "package.json scripts"
      to: "npm run lint"
      via: "npm run lint:py && npm run lint:js"
      pattern: "lint:py.*&&.*lint:js"
---

<objective>
Independently verify Phase 13 (Linting Baseline) by running all lint commands, inspecting configuration artifacts, performing a violation detection test, and writing 13-VERIFICATION.md.

Purpose: Phase 13 was implemented (plans 13-01 and 13-02 complete with SUMMARYs) but the v2.0 milestone audit found it was never verified by gsd-verifier. This gap closure phase produces the missing VERIFICATION.md to satisfy LINT-01 through LINT-04.

Output: `.planning/phases/13-linting-baseline/13-VERIFICATION.md` in the standard format established by 12-VERIFICATION.md and 15-VERIFICATION.md.
</objective>

<execution_context>
@/Users/annon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/annon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v2.0-MILESTONE-AUDIT.md
@.planning/phases/16-verify-linting-baseline/16-RESEARCH.md
@.planning/phases/13-linting-baseline/13-01-SUMMARY.md
@.planning/phases/13-linting-baseline/13-02-SUMMARY.md
@.planning/phases/12-python-unit-tests/12-VERIFICATION.md
@.planning/phases/15-pre-push-hook/15-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run lint verification commands and violation detection test</name>
  <files>
    (no files modified — observation-only task)
  </files>
  <action>
Run all three lint commands and capture their exact output for evidence:

1. `npm run lint:py` — expect "All checks passed!" + exit 0
2. `npm run lint:js` — expect no output + exit 0
3. `npm run lint` — expect both linters pass in sequence + exit 0

Then perform the violation detection test (LINT-01 success criteria #4 from ROADMAP.md):

4. Add `import os` (unused) to top of `pipeline/config.py`
5. Run `npm run lint:py` — expect FAILURE with ruff violation pointing to file and line (F401 unused import)
6. Remove the added `import os` line to restore the file
7. Run `npm run lint:py` again to confirm clean state restored

Also verify artifact contents:
8. Inspect `pyproject.toml` [tool.ruff] section — confirm select = ["E", "F", "W", "B", "I", "UP"], line-length = 88
9. Inspect `eslint.config.js` — confirm flat config with sourceType: "script", IIFE globals, max-len 88
10. Inspect `package.json` scripts — confirm lint:py = "ruff check pipeline/ tests/", lint:js = "npx eslint public/js/", lint = "npm run lint:py && npm run lint:js"
11. Check no inline suppressions: grep for "noqa" in pipeline/ and tests/, grep for "eslint-disable" in public/js/

Record all output strings, exit codes, and configuration values. These become the Evidence column in the VERIFICATION.md.
  </action>
  <verify>
    <automated>npm run lint</automated>
    <manual>All three lint commands exit 0; violation detection test shows F401 when unused import added and clean pass after removal</manual>
  </verify>
  <done>All lint commands confirmed passing; violation detection test confirms ruff catches an introduced violation; artifact configurations inspected and values recorded</done>
</task>

<task type="auto">
  <name>Task 2: Write 13-VERIFICATION.md with evidence from Task 1</name>
  <files>.planning/phases/13-linting-baseline/13-VERIFICATION.md</files>
  <action>
Create `.planning/phases/13-linting-baseline/13-VERIFICATION.md` following the exact format from 12-VERIFICATION.md and 15-VERIFICATION.md.

**CRITICAL:** The file goes in the Phase 13 directory (the phase being verified), NOT in the Phase 16 directory.

Frontmatter:
```yaml
---
phase: 13-linting-baseline
verified: [current ISO timestamp]
status: passed
score: 4/4 requirements verified
re_verification: true
---
```

Note: `re_verification: true` because Phase 13 was completed but never verified — this is gap-closure re-verification per the audit.

Document structure (follow 12-VERIFICATION.md template exactly):

1. **Header** — Phase Goal from ROADMAP: "Python and JavaScript linting runs clean on the entire codebase with no pre-existing violations suppressed"
2. **Observable Truths** — 4 truths matching Phase 13 success criteria:
   - Truth 1: `npm run lint:py` reports 0 violations (LINT-01 + LINT-02)
   - Truth 2: `npm run lint:js` reports 0 violations (LINT-03)
   - Truth 3: `npm run lint` both pass in sequence (LINT-04)
   - Truth 4: Introducing unused import causes lint:py to fail with violation message (regression detection)
   - Use exact output captured in Task 1 as evidence
3. **Required Artifacts** — 3 artifacts:
   - `pyproject.toml` — [tool.ruff] config with select E/F/W/B/I/UP, line-length 88
   - `eslint.config.js` — flat config with sourceType script, IIFE globals, max-len 88
   - `package.json` — lint:py, lint:js, lint scripts defined
4. **Key Link Verification** — 3 links:
   - pyproject.toml [tool.ruff] → npm run lint:py (via ruff check pipeline/ tests/)
   - eslint.config.js → npm run lint:js (via npx eslint public/js/)
   - package.json lint → lint:py + lint:js (via sequential && composition)
5. **Requirements Coverage** — 4 requirements:
   - LINT-01: Python passes ruff checks — evidence from lint:py output
   - LINT-02: Python violations cleaned in baseline — reference commit 66c3c02 from 13-01-SUMMARY, plus current zero-violation state
   - LINT-03: JavaScript passes ESLint v10 — evidence from lint:js output
   - LINT-04: Linting via npm scripts — all three scripts confirmed working
6. **Anti-Patterns Found** — Check for: no inline noqa suppressions in Python, no eslint-disable in JS, no TODO/FIXME in config files
7. **Human Verification Required** — None expected (all verification is automated)
8. **Summary** — Phase 13 goal fully achieved; all 4 LINT requirements satisfied
9. **Commit Evidence** — Reference commits 66c3c02 (ruff cleanup) and cb40ef4 (ESLint setup) from SUMMARY files

Footer: `_Verified: [timestamp]_` / `_Verifier: Claude (gsd-verifier)_`
  </action>
  <verify>
    <automated>test -f .planning/phases/13-linting-baseline/13-VERIFICATION.md && head -5 .planning/phases/13-linting-baseline/13-VERIFICATION.md | grep -q "phase: 13-linting-baseline"</automated>
    <manual>VERIFICATION.md exists in phase 13 directory (not phase 16), has correct frontmatter with re_verification: true, all 4 LINT requirements listed as SATISFIED</manual>
  </verify>
  <done>13-VERIFICATION.md exists in .planning/phases/13-linting-baseline/ with all 4 Observable Truths verified, all 4 LINT requirements marked SATISFIED, and commit evidence from Phase 13 referenced</done>
</task>

</tasks>

<verification>
Phase 16 is complete when:
1. `npm run lint:py` exits 0 (confirming LINT-01 + LINT-02)
2. `npm run lint:js` exits 0 (confirming LINT-03)
3. `npm run lint` exits 0 (confirming LINT-04)
4. Violation detection test performed and documented (LINT-01 success criteria #4)
5. `.planning/phases/13-linting-baseline/13-VERIFICATION.md` exists with status: passed and all 4 requirements SATISFIED
</verification>

<success_criteria>
- 13-VERIFICATION.md exists in `.planning/phases/13-linting-baseline/` (correct directory)
- Frontmatter has `re_verification: true` and `score: 4/4 requirements verified`
- All 4 Observable Truths have VERIFIED status with captured command output as evidence
- All 4 LINT requirements (LINT-01 through LINT-04) listed as SATISFIED with specific evidence
- No inline suppressions (noqa or eslint-disable) found in codebase
- Violation detection test confirms ruff catches introduced violations
</success_criteria>

<output>
After completion, create `.planning/phases/16-verify-linting-baseline/16-01-SUMMARY.md`
</output>
