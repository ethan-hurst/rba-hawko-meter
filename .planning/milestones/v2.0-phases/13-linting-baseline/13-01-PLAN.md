---
phase: 13-linting-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pipeline/ingest/abs_data.py
  - pipeline/ingest/asx_futures_scraper.py
  - pipeline/ingest/corelogic_scraper.py
  - pipeline/ingest/nab_scraper.py
  - pipeline/ingest/rba_data.py
  - pipeline/normalize/engine.py
  - pipeline/normalize/ratios.py
  - pipeline/normalize/zscore.py
  - pipeline/normalize/gauge.py
  - pipeline/config.py
  - pipeline/main.py
  - pipeline/utils/csv_handler.py
  - pipeline/utils/http_client.py
  - tests/python/conftest.py
  - tests/python/test_smoke.py
  - tests/python/test_zscore.py
  - tests/python/test_gauge.py
  - tests/python/test_ratios.py
  - tests/python/test_csv_handler.py
  - tests/python/test_schema.py
autonomous: true
requirements:
  - LINT-01
  - LINT-02

must_haves:
  truths:
    - "Developer runs `ruff check pipeline/ tests/` and gets 0 violations"
    - "No inline suppressions (noqa, type: ignore) were added — every violation was actually fixed"
    - "All existing tests still pass after the cleanup (no behavioral changes)"
  artifacts:
    - path: "pipeline/"
      provides: "Ruff-clean Python source code"
      contains: "No E501, F, W, B, I, UP violations"
    - path: "tests/python/"
      provides: "Ruff-clean Python test code"
      contains: "No E501, F, W, B, I, UP violations"
  key_links:
    - from: "pyproject.toml [tool.ruff.lint]"
      to: "pipeline/ and tests/"
      via: "ruff check enforces select = E,F,W,B,I,UP"
      pattern: "select = \\[\"E\", \"F\", \"W\", \"B\", \"I\", \"UP\"\\]"
---

<objective>
Clean all pre-existing ruff violations across the Python codebase so that `ruff check pipeline/ tests/` exits 0.

Purpose: LINT-01 and LINT-02 require zero violations before the pre-push hook (Phase 15) can gate on linting. The baseline cleanup absorbs all existing technical debt in a single commit.

Output: All Python files in `pipeline/` and `tests/python/` pass ruff with 0 violations. One cleanup commit.
</objective>

<execution_context>
@/Users/annon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/annon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-linting-baseline/13-RESEARCH.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auto-fix ruff violations and manually fix B904</name>
  <files>
    pipeline/ingest/abs_data.py
    pipeline/ingest/asx_futures_scraper.py
    pipeline/ingest/corelogic_scraper.py
    pipeline/ingest/nab_scraper.py
    pipeline/ingest/rba_data.py
    pipeline/normalize/engine.py
    pipeline/normalize/ratios.py
    pipeline/normalize/zscore.py
    pipeline/normalize/gauge.py
    pipeline/config.py
    pipeline/main.py
    pipeline/utils/csv_handler.py
    pipeline/utils/http_client.py
    tests/python/conftest.py
    tests/python/test_smoke.py
    tests/python/test_zscore.py
    tests/python/test_gauge.py
    tests/python/test_ratios.py
    tests/python/test_csv_handler.py
    tests/python/test_schema.py
  </files>
  <action>
Run ruff auto-fix across the full Python codebase:

```bash
ruff check pipeline/ tests/ --fix
```

This handles ~46 auto-fixable violations plus UP035 typing import deprecations (which ruff resolves in multi-pass mode when usage sites are also modernized by UP006/UP007/UP045 fixes).

After auto-fix, manually fix the B904 violation in `pipeline/ingest/abs_data.py:59`:
- Before: `raise Exception(f"Failed to parse CSV response: {e}\nPreview: {response.text[:500]}")`
- After: `raise Exception(f"Failed to parse CSV response: {e}\nPreview: {response.text[:500]}") from e`

Then run `ruff check pipeline/ tests/` to see remaining violations. At this point, only E501 (line-too-long, max 88 chars) violations should remain — approximately 90 across pipeline/ and tests/.

IMPORTANT constraints (from CONTEXT.md):
- Do NOT add any `# noqa` or `# type: ignore` inline suppressions — every violation must be actually fixed
- Do NOT run `ruff format` — lint only, no formatting enforcement
- Do NOT change ruff configuration in pyproject.toml — the rules and target-version are already correct
- Leave `target-version = "py313"` as-is (do not downgrade to py310 despite CONTEXT.md mentioning 3.10 for UP rules; see research pitfall 6)
  </action>
  <verify>
    <automated>cd /Users/annon/projects/rba-hawko-meter && ruff check pipeline/ tests/ --output-format=concise 2>&1 | grep -c "E501"</automated>
    <manual>Only E501 violations should remain after this task. Verify no other rule codes appear in output.</manual>
  </verify>
  <done>All auto-fixable ruff violations resolved. Only E501 (line-too-long) violations remain. B904 in abs_data.py manually fixed with `from e` clause.</done>
</task>

<task type="auto">
  <name>Task 2: Manually fix all E501 line-too-long violations</name>
  <files>
    pipeline/ingest/abs_data.py
    pipeline/ingest/asx_futures_scraper.py
    pipeline/ingest/corelogic_scraper.py
    pipeline/ingest/nab_scraper.py
    pipeline/ingest/rba_data.py
    pipeline/normalize/engine.py
    pipeline/normalize/ratios.py
    pipeline/normalize/zscore.py
    pipeline/normalize/gauge.py
    pipeline/config.py
    pipeline/main.py
    pipeline/utils/csv_handler.py
    pipeline/utils/http_client.py
    tests/python/conftest.py
    tests/python/test_smoke.py
    tests/python/test_zscore.py
    tests/python/test_gauge.py
    tests/python/test_ratios.py
    tests/python/test_csv_handler.py
    tests/python/test_schema.py
  </files>
  <action>
Fix all remaining E501 violations (lines exceeding 88 characters). Approximately 90 violations across pipeline/ and tests/ (some new ones may have been introduced by UP rule auto-fixes that expand type annotations).

Run `ruff check pipeline/ tests/ --output-format=concise` to get the exact list with file:line references.

Line-wrapping strategies (use as appropriate per context):
1. **Long strings**: Use implicit string concatenation or parenthesized line continuation
   ```python
   msg = (
       f"Failed to parse: {e}\n"
       f"Preview: {text[:500]}"
   )
   ```
2. **Long function calls**: Break after opening paren, one argument per line
   ```python
   result = some_function(
       first_arg,
       second_arg,
       third_arg,
   )
   ```
3. **Long dict/list literals**: One item per line with trailing comma
4. **Long conditionals**: Wrap in parens, break at logical operators
5. **Long imports**: Use parenthesized multi-line imports (already handled by I rules)
6. **Long comments**: Rewrap to stay under 88 chars
7. **Long docstrings in tests**: Wrap at logical sentence boundaries
8. **Long URLs in comments**: Place URL on its own line (URLs cannot be wrapped)

IMPORTANT: Do NOT change any logic or behavior — this is purely formatting. After wrapping, ensure the code is still syntactically correct by running `python -c "import pipeline.config"` as a quick syntax check.

After all fixes, run the full test suite to confirm no behavioral changes:
```bash
pytest tests/python/ -m "not live" -x
```
  </action>
  <verify>
    <automated>cd /Users/annon/projects/rba-hawko-meter && ruff check pipeline/ tests/ && pytest tests/python/ -m "not live" -x --tb=short -q</automated>
    <manual>Verify ruff reports "All checks passed!" and all existing tests pass.</manual>
  </verify>
  <done>All E501 violations fixed. `ruff check pipeline/ tests/` exits 0 with no violations. All existing pytest tests still pass. One commit created for entire ruff baseline cleanup.</done>
</task>

</tasks>

<verification>
1. `ruff check pipeline/ tests/` exits 0 with "All checks passed!" message
2. `pytest tests/python/ -m "not live" -x` passes all existing tests (no regressions from formatting changes)
3. `grep -r "# noqa" pipeline/ tests/python/` returns nothing (no inline suppressions)
4. `grep -r "# type: ignore" pipeline/ tests/python/` returns nothing (no type suppressions)
</verification>

<success_criteria>
- `ruff check pipeline/ tests/` reports zero violations
- All pytest tests pass unchanged
- No inline suppressions added anywhere
- Changes committed as a single ruff baseline cleanup commit
</success_criteria>

<output>
After completion, create `.planning/phases/13-linting-baseline/13-01-SUMMARY.md`
</output>
