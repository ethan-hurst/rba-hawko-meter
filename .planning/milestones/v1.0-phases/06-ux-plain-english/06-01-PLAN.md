---
phase: 06-ux-plain-english
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/js/gauges.js
  - public/js/interpretations.js
  - public/index.html
autonomous: true

must_haves:
  truths:
    - "Gauge zone labels say 'RATES LIKELY FALLING' through 'RATES LIKELY RISING' instead of 'DOVISH'/'HAWKISH'"
    - "Hero gauge number displays as '46/100' not bare '46'"
    - "Bullet gauge numbers display '/100' suffix"
    - "Onboarding explainer is visible between header and hero gauge"
    - "Scale explainer text appears beneath the verdict"
  artifacts:
    - path: "public/js/gauges.js"
      provides: "Plain English zone labels and /100 suffix on all gauges"
      contains: "RATES LIKELY"
    - path: "public/js/interpretations.js"
      provides: "Updated ZONE_LABEL_MAP with plain English labels"
      contains: "RATES LIKELY"
    - path: "public/index.html"
      provides: "Onboarding section and scale explainer"
      contains: "What is the Hawk-O-Meter"
  key_links:
    - from: "public/js/gauges.js"
      to: "public/js/interpretations.js"
      via: "ZONE_COLORS labels must match ZONE_LABEL_MAP values"
      pattern: "RATES LIKELY"
    - from: "public/js/interpretations.js"
      to: "public/index.html#verdict-container"
      via: "renderVerdict uses ZONE_LABEL_MAP to populate verdict"
      pattern: "ZONE_LABEL_MAP"
---

<objective>
Replace economist jargon with plain English across all gauge labels and add contextual onboarding so that a first-time visitor understands the 0-100 score and its meaning within 5 seconds.

Purpose: The P0 critical fixes from the UX audit - zone labels, score units, onboarding, and scale explanation are the minimum changes to transform the dashboard from "for economists" to "for mortgage holders".

Output: Updated gauges.js, interpretations.js, and index.html with plain English labels, /100 suffixes, onboarding section, and scale explainer.
</objective>

<execution_context>
@/Users/annon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/annon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ux-plain-english/06-CONTEXT.md
@.planning/phases/06-ux-plain-english/06-RESEARCH.md
@public/js/gauges.js
@public/js/interpretations.js
@public/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace zone labels with plain English in gauges.js and interpretations.js</name>
  <files>public/js/gauges.js, public/js/interpretations.js</files>
  <action>
  In `public/js/gauges.js`, update the ZONE_COLORS array (lines 9-15) label values:
  - `'STRONGLY DOVISH'` -> `'RATES LIKELY FALLING'`
  - `'DOVISH'` -> `'LEANING TOWARDS CUTS'`
  - `'NEUTRAL'` -> `'HOLDING STEADY'`
  - `'HAWKISH'` -> `'LEANING TOWARDS RISES'`
  - `'STRONGLY HAWKISH'` -> `'RATES LIKELY RISING'`

  In `public/js/interpretations.js`, update the ZONE_LABEL_MAP values (lines 8-14) to match:
  - `'Strong dovish pressure': 'RATES LIKELY FALLING'`
  - `'Mild dovish pressure': 'LEANING TOWARDS CUTS'`
  - `'Mild hawkish pressure': 'LEANING TOWARDS RISES'`
  - `'Strong hawkish pressure': 'RATES LIKELY RISING'`
  - `'Balanced': 'HOLDING STEADY'`

  These two maps MUST stay in sync -- the ZONE_LABEL_MAP values must exactly match the ZONE_COLORS label strings.

  Do NOT change colors, ranges, function signatures, or any other logic. Label text only.
  </action>
  <verify>
  `grep -n "RATES LIKELY" public/js/gauges.js` shows 2 matches (lines ~10 and ~14).
  `grep -n "HOLDING STEADY" public/js/gauges.js` shows 1 match (line ~12).
  `grep -n "RATES LIKELY" public/js/interpretations.js` shows 2 matches.
  `grep -n "DOVISH\|HAWKISH" public/js/gauges.js public/js/interpretations.js` returns NO matches (old labels fully removed).
  </verify>
  <done>All five zone labels in both files use plain English. No trace of "DOVISH" or "HAWKISH" remains in either file.</done>
</task>

<task type="auto">
  <name>Task 2: Add "/100" suffix to hero and bullet gauge numbers</name>
  <files>public/js/gauges.js</files>
  <action>
  In `public/js/gauges.js`, add the `suffix` property to the `number` object in three locations:

  1. **createHeroGauge** (line ~119-122): Add `suffix: '/100'` to the number config:
     ```js
     number: {
         font: { size: 52, color: '#f3f4f6' },
         valueformat: '.0f',
         suffix: '/100'
     }
     ```

  2. **updateHeroGauge** (line ~164-167): Same change -- add `suffix: '/100'`:
     ```js
     number: {
         font: { size: 52, color: '#f3f4f6' },
         valueformat: '.0f',
         suffix: '/100'
     }
     ```

  3. **createBulletGauge** (line ~207-211): Change empty suffix to `/100`:
     ```js
     number: {
         font: { size: 24, color: '#f3f4f6' },
         valueformat: '.0f',
         suffix: '/100'
     }
     ```

  Do NOT change font sizes, colors, valueformat, or any gauge geometry. Only add/change the suffix property.
  </action>
  <verify>
  `grep -c "suffix: '/100'" public/js/gauges.js` returns 3 (one per gauge creation/update function).
  `grep -c "suffix: ''" public/js/gauges.js` returns 0 (no empty suffixes remain).
  </verify>
  <done>Hero gauge shows "46/100", bullet gauges show "73/100" etc. All three gauge render paths include the /100 suffix.</done>
</task>

<task type="auto">
  <name>Task 3: Add onboarding explainer section in index.html</name>
  <files>public/index.html</files>
  <action>
  Insert a new `<section>` as the FIRST child of `<main>` (after line 70 `<main class="flex-1...">`, before line 73 `<section id="hawk-o-meter-section">`).

  Use the HTML5 `<details>/<summary>` pattern for collapsibility (no JS needed, accessible by default). Style with existing Tailwind utility classes matching the project's dark theme.

  The section should contain:

  ```html
  <section id="onboarding" aria-label="About this dashboard" class="mb-6">
    <details class="bg-finance-gray border border-finance-border rounded-xl px-6 py-4" open>
      <summary class="cursor-pointer text-lg font-semibold text-gray-200 select-none">
        What is the Hawk-O-Meter?
      </summary>
      <div class="mt-3 space-y-2 text-sm text-gray-400">
        <p>
          The Hawk-O-Meter tracks publicly available economic data that the Reserve Bank of Australia (RBA) considers when setting the cash rate. It combines these indicators into a single score out of 100.
        </p>
        <p>
          <span class="text-blue-400 font-medium">Below 50</span> = economic data suggests pressure to <strong class="text-gray-200">cut</strong> interest rates.
          <span class="text-red-400 font-medium">Above 50</span> = data suggests pressure to <strong class="text-gray-200">raise</strong> them.
        </p>
        <p>
          This is not a prediction or financial advice. It is a mathematical summary of publicly available data.
        </p>
      </div>
    </details>
  </section>
  ```

  Key requirements:
  - Use `<details open>` so it is expanded on first visit
  - ASIC RG 244 compliant: no advice language, includes "not a prediction or financial advice" disclaimer
  - Uses existing Tailwind theme classes (`bg-finance-gray`, `border-finance-border`, `text-gray-200/400`)
  - No innerHTML, no JS -- pure semantic HTML
  </action>
  <verify>
  `grep -n "What is the Hawk-O-Meter" public/index.html` returns 1 match in the onboarding section.
  `grep -n "id=\"onboarding\"" public/index.html` returns 1 match.
  `grep -n "not a prediction or financial advice" public/index.html` confirms ASIC-safe language.
  Open index.html in browser: onboarding section is visible between header and hero gauge, and can be collapsed by clicking the summary.
  </verify>
  <done>Onboarding section exists between header and hero gauge. It explains what the Hawk-O-Meter is, what the score means, and includes an ASIC-compliant disclaimer. Collapsible via native details/summary.</done>
</task>

<task type="auto">
  <name>Task 4: Add scale explainer beneath the verdict container</name>
  <files>public/index.html</files>
  <action>
  Insert a scale explainer `<p>` element AFTER the `<div id="verdict-container">` (line 99-101) and BEFORE the closing `</section>` tag of the hero section (line 102).

  ```html
  <p id="scale-explainer" class="mt-2 text-center text-sm text-gray-500">
    Score out of 100. Below 50 = pressure to cut rates. Above 50 = pressure to raise rates.
  </p>
  ```

  This is a static HTML element -- no JS needed. Uses muted `text-gray-500` to avoid visual competition with the verdict text above it.
  </action>
  <verify>
  `grep -n "scale-explainer" public/index.html` returns 1 match.
  `grep -n "Score out of 100" public/index.html` returns 1 match.
  The scale explainer appears directly below the verdict line in the browser.
  </verify>
  <done>Scale explainer text "Score out of 100. Below 50 = pressure to cut rates. Above 50 = pressure to raise rates." is visible beneath the verdict, styled in muted gray.</done>
</task>

</tasks>

<verification>
1. `grep -r "DOVISH\|HAWKISH" public/js/` returns NO results (old jargon fully removed from JS)
2. `grep -c "suffix: '/100'" public/js/gauges.js` returns 3
3. `grep "What is the Hawk-O-Meter" public/index.html` returns 1 match
4. `grep "Score out of 100" public/index.html` returns 1 match
5. Open `public/index.html` in browser:
   - Onboarding section visible between header and hero gauge
   - Hero gauge shows "46/100" with label "HOLDING STEADY" (not "NEUTRAL")
   - Verdict shows plain English label (e.g., "HOLDING STEADY" not "NEUTRAL")
   - Scale explainer visible below verdict
   - Bullet gauges show "/100" suffix
</verification>

<success_criteria>
- Zero instances of "DOVISH" or "HAWKISH" in any JS file
- All gauge numbers display "/100" suffix
- First-time visitor sees collapsible "What is the Hawk-O-Meter?" explainer before the gauge
- Scale context ("Score out of 100...") is visible beneath the verdict
- No ASIC RG 244 compliance regressions (no advice language introduced)
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-plain-english/06-01-SUMMARY.md`
</output>
