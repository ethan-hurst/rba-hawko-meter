---
phase: 06-ux-plain-english
plan: 02
type: execute
wave: 1
depends_on: ["06-01"]
files_modified:
  - public/js/interpretations.js
  - public/js/gauge-init.js
  - public/index.html
autonomous: true

must_haves:
  truths:
    - "Building approvals card never shows -99.9% to users"
    - "Each metric card explains in plain English what the data means (no jargon like CPI, YoY)"
    - "Each metric card has a one-liner explaining why that indicator matters for interest rates"
    - "Weight badges say High/Medium/Lower importance instead of raw percentages"
    - "Stale data shows age in months with context, not just (stale)"
    - "Verdict text answers what this means for mortgages, not just economic indicators are balanced"
    - "Dates show Australian format (1 Dec 2025) not ISO (2025-12-01)"
    - "Data coverage notice shows how many of 8 indicators are available"
  artifacts:
    - path: "public/js/interpretations.js"
      provides: "Plain English interpretations, data quality guard, weight badges, stale labels, verdict, why-it-matters, date formatting, coverage notice"
  key_links:
    - from: "public/js/interpretations.js:generateMetricInterpretation"
      to: "public/js/interpretations.js:renderMetricCard"
      via: "renderMetricCard calls generateMetricInterpretation for each card"
      pattern: "generateMetricInterpretation\\(metricId"
    - from: "public/js/interpretations.js:renderMetricCard"
      to: "public/js/gauge-init.js:renderMetricGauges"
      via: "renderMetricGauges calls renderMetricCard for each indicator"
      pattern: "InterpretationsModule\\.renderMetricCard"
    - from: "public/js/interpretations.js:renderVerdict"
      to: "public/js/gauge-init.js:initGauges"
      via: "initGauges calls renderVerdict with data.overall"
      pattern: "InterpretationsModule\\.renderVerdict"
---

<objective>
Rewrite all user-facing text in metric cards and verdict to plain Australian English, add data quality guards for bogus values, and add contextual helpers (why-it-matters, coverage notice, human-readable dates).

Purpose: Users currently see jargon ("CPI at 1.4% YoY", "25% weight", "(stale)") and a bogus "-99.9% YoY" for building approvals. This plan eliminates every instance of economist-speak from the metric cards and verdict, adds safety guards for bad data, and gives users context on why each indicator matters for their mortgage.

Output: Updated `interpretations.js` with plain English throughout, data quality guard, and supporting helpers. Updated `gauge-init.js` with data coverage notice. Updated `index.html` with coverage notice container.
</objective>

<execution_context>
@/Users/annon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/annon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ux-plain-english/06-CONTEXT.md
@public/js/interpretations.js
@public/js/gauge-init.js
@public/js/gauges.js
@public/data/status.json
@public/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add data quality guard and rewrite generateMetricInterpretation</name>
  <files>public/js/interpretations.js</files>
  <action>
  **Part A: Data quality guard for building approvals**

  Add a helper function `isDataSuspect(metricId, metricData)` near the top of the IIFE (after the `percentFormatter` declaration around line 20). This function checks for bogus data values that should not be shown to users:

  ```
  function isDataSuspect(metricId, metricData) {
    if (metricId === 'building_approvals') {
      var raw = parseFloat(metricData.raw_value);
      if (isNaN(raw) || raw < -90 || raw > 500) return true;
    }
    return false;
  }
  ```

  **Part B: Rewrite generateMetricInterpretation (lines 184-228)**

  Replace the entire function body with plain English interpretations. At the top of the function, check `isDataSuspect` first:

  ```
  if (isDataSuspect(metricId, metricData)) {
    return 'Building approvals data is currently being updated';
  }
  ```

  Then rewrite each case to drop jargon:

  - `inflation`:
    - v < 40: `'Prices up ' + raw + '% over the past year \u2014 within the RBA\u2019s 2\u20133% target'`
    - v <= 60: `'Prices up ' + raw + '% over the past year \u2014 near the top of the RBA\u2019s 2\u20133% target'`
    - v > 60: `'Prices up ' + raw + '% over the past year \u2014 above the RBA\u2019s 2\u20133% target'`

  - `wages`:
    - v < 40: `'Wages up ' + raw + '% over the past year \u2014 growing slowly'`
    - v <= 60: `'Wages up ' + raw + '% over the past year \u2014 moderate growth'`
    - v > 60: `'Wages up ' + raw + '% over the past year \u2014 growing fast, which can push up prices'`

  - `building_approvals`:
    - v < 40: `'New building approvals are below average \u2014 a sign of slowing construction'`
    - v <= 60: `'New building approvals are near average levels'`
    - v > 60: `'New building approvals are above average \u2014 a sign of strong construction demand'`

  - `housing`:
    - v < 40: `'House prices are easing \u2014 less pressure on affordability'`
    - v <= 60: `'House prices are growing at a moderate pace'`
    - v > 60: `'House prices are rising strongly \u2014 adding to affordability pressure'`

  - `employment`:
    - v < 40: `'The job market is softening \u2014 fewer new jobs being created'`
    - v <= 60: `'The job market is steady \u2014 balanced conditions'`
    - v > 60: `'The job market is very tight \u2014 lots of demand for workers, which can push up wages'`

  - `spending`:
    - v < 40: `'Consumer spending is subdued \u2014 people are holding back'`
    - v <= 60: `'Consumer spending is at moderate levels'`
    - v > 60: `'Consumer spending is strong \u2014 which can push up prices'`

  - `business_confidence`:
    - v < 40: `'Business confidence is below average \u2014 businesses are cautious'`
    - v <= 60: `'Business confidence is around average levels'`
    - v > 60: `'Business confidence is high \u2014 businesses are investing and hiring more'`

  - `default`: Keep existing fallback.

  Note: For building_approvals, do NOT include raw values in the text since the underlying data (YoY%) is unreliable. Use qualitative descriptions only based on the gauge value (which is derived from the z-score, which IS reliable).

  **Part C: Add getWhyItMatters helper**

  Add a new function after generateMetricInterpretation:

  ```
  function getWhyItMatters(metricId) {
    var reasons = {
      inflation: 'When prices rise too fast, the RBA tends to raise interest rates to slow things down.',
      wages: 'Rising wages can push up prices, which can lead the RBA to raise rates.',
      building_approvals: 'Fewer approvals signals a slowing economy, which may support rate cuts.',
      housing: 'Rapidly rising house prices can prompt the RBA to raise rates to cool the market.',
      employment: 'Low unemployment means a strong economy, which can lead to rate rises.',
      spending: 'When consumers spend more, it can push prices up and lead to rate rises.',
      business_confidence: 'When businesses are confident, they invest and hire more, adding to inflation pressure.'
    };
    return reasons[metricId] || null;
  }
  ```

  **Part D: Add formatAusDate helper**

  Add a helper to format ISO dates as Australian format:

  ```
  var ausDateFormatter = new Intl.DateTimeFormat('en-AU', {
    day: 'numeric', month: 'short', year: 'numeric'
  });

  function formatAusDate(isoDateStr) {
    if (!isoDateStr) return 'unknown';
    var d = new Date(isoDateStr);
    if (isNaN(d.getTime())) return isoDateStr;
    return ausDateFormatter.format(d);
  }
  ```

  **Part E: Add getPlainVerdict helper**

  Add a function that returns actionable, ASIC-compliant verdict text based on the hawk score. This replaces the current "Economic indicators are broadly balanced" style text.

  ```
  function getPlainVerdict(score) {
    if (score < 20) {
      return 'The economy is showing significant signs of slowing. Interest rates are more likely to come down.';
    }
    if (score < 40) {
      return 'The economy is showing some signs of easing. Interest rates may be more likely to fall than rise.';
    }
    if (score <= 60) {
      return 'The economy is giving mixed signals. Interest rates are likely to stay where they are for now.';
    }
    if (score <= 80) {
      return 'The economy is running warm. Interest rates may be more likely to rise than fall.';
    }
    return 'The economy is running hot. Interest rates are more likely to go up.';
  }
  ```

  Use careful language: "likely to", "may be more likely", "showing signs of" -- NEVER "will", "should", "we recommend". This is critical for ASIC RG 244 compliance.

  Export the new functions in the return statement: add `getWhyItMatters`, `formatAusDate`, `getPlainVerdict`, and `isDataSuspect` alongside existing exports.
  </action>
  <verify>
  Open the browser console and test:
  - `InterpretationsModule.generateMetricInterpretation('inflation', {value: 47.3, raw_value: 1.43, raw_unit: '% YoY'})` returns text starting with "Prices up"
  - `InterpretationsModule.generateMetricInterpretation('building_approvals', {value: 38.7, raw_value: -99.91, raw_unit: '% YoY'})` returns "Building approvals data is currently being updated"
  - `InterpretationsModule.isDataSuspect('building_approvals', {raw_value: -99.91})` returns true
  - `InterpretationsModule.isDataSuspect('inflation', {raw_value: 1.43})` returns false
  - `InterpretationsModule.getWhyItMatters('inflation')` returns the one-liner
  - `InterpretationsModule.formatAusDate('2025-12-01')` returns "1 Dec 2025"
  - `InterpretationsModule.getPlainVerdict(46)` returns text about mixed signals
  - No `innerHTML` usage in the file: `grep -c 'innerHTML' public/js/interpretations.js` returns 0
  </verify>
  <done>
  All interpretation text uses plain English (no CPI, YoY, target band jargon). Building approvals with raw_value -99.91 shows "data is currently being updated". getWhyItMatters returns one-liners for all 7 indicators. formatAusDate converts ISO dates to Australian format. getPlainVerdict returns ASIC-compliant mortgage-relevant verdict text.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite renderMetricCard for weight badges, stale labels, why-it-matters, and Australian dates</name>
  <files>public/js/interpretations.js</files>
  <action>
  Modify `renderMetricCard` (lines 237-299) to incorporate the new helpers:

  **Part A: Replace weight badge (line 257-259)**

  Replace the weight badge text logic. Instead of `Math.round(weight * 100) + '% weight'`, use an importance label with hover title:

  ```
  var pct = Math.round(weight * 100);
  var importanceLabel;
  if (pct >= 20) importanceLabel = 'High importance';
  else if (pct >= 10) importanceLabel = 'Medium importance';
  else importanceLabel = 'Lower importance';

  weightBadge.textContent = importanceLabel;
  weightBadge.title = pct + '% of overall score';
  ```

  **Part B: Replace "(stale)" label (lines 290-295)**

  Replace the stale indicator logic. Instead of just "(stale)", calculate months and show a descriptive message:

  ```
  if (stale) {
    var months = Math.round(metricData.staleness_days / 30);
    var staleNote = document.createElement('span');
    staleNote.className = 'text-amber-400 ml-2';
    staleNote.textContent = '(' + months + ' month' + (months !== 1 ? 's' : '') + ' old \u2014 newer data not yet available)';
    sourceDiv.appendChild(staleNote);
  }
  ```

  **Part C: Format dates as Australian (line 289)**

  Change the source citation date from ISO format to Australian format:

  Replace:
  `sourceDiv.textContent = 'Data as of ' + (metricData.data_date || 'unknown');`

  With:
  `sourceDiv.textContent = 'Data as of ' + formatAusDate(metricData.data_date);`

  **Part D: Add "why it matters" one-liner after interpretation text (after line 284)**

  After appending interpDiv to the card, add the why-it-matters line:

  ```
  var whyText = getWhyItMatters(metricId);
  if (whyText) {
    var whyDiv = document.createElement('div');
    whyDiv.className = 'text-xs text-gray-500 mt-1 italic';
    whyDiv.textContent = whyText;
    card.appendChild(whyDiv);
  }
  ```

  This goes between interpDiv and sourceDiv in the card structure.
  </action>
  <verify>
  Reload the page and inspect each metric card:
  - Inflation card shows "High importance" badge (25% weight) with hover title "25% of overall score"
  - Wages card shows "Medium importance" badge (15% weight) with hover title "15% of overall score"
  - Building Approvals card shows "Lower importance" badge (5% weight)
  - Wages card (220 staleness_days) shows "(7 months old -- newer data not yet available)" in amber
  - Each card shows a "Why it matters" line in italic gray text below the interpretation
  - Inflation card date shows "1 Dec 2025" not "2025-12-01"
  - Building Approvals card date shows "31 Dec 2025" not "2025-12-31"
  </verify>
  <done>
  Weight badges show importance labels with percentage on hover. Stale data shows age in months with "newer data not yet available" context. Source dates display in Australian format. Each metric card includes a "why it matters" one-liner explaining the indicator's relevance to interest rates.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite renderVerdict to use plain English and add data coverage notice</name>
  <files>public/js/interpretations.js, public/js/gauge-init.js, public/index.html</files>
  <action>
  **Part A: Update renderVerdict in interpretations.js (lines 27-53)**

  Modify `renderVerdict` to use `getPlainVerdict` when receiving an object. Replace the `dashSpan.textContent` line:

  Current (line 49):
  ```
  dashSpan.textContent = ' \u2014 ' + (overallData.verdict || '');
  ```

  Replace with:
  ```
  var plainVerdict = getPlainVerdict(overallData.hawk_score);
  dashSpan.textContent = ' \u2014 ' + plainVerdict;
  ```

  This uses the hawk_score to generate the plain English verdict instead of the pipeline-generated "Economic indicators are broadly balanced" text.

  **Part B: Add data coverage notice container in index.html**

  In `public/index.html`, find the Economic Indicators section header (line 122):

  ```html
  <h2 class="text-xl font-semibold text-gray-200 mb-4">Economic Indicators</h2>
  ```

  Add a `<p>` element with id `data-coverage-notice` right after the `<h2>`:

  ```html
  <p id="data-coverage-notice" class="text-sm text-gray-400 mb-4"></p>
  ```

  **Part C: Render data coverage notice in gauge-init.js**

  In `renderMetricGauges` in `public/js/gauge-init.js`, after the `orderedIds` list is built (after line 43), add logic to populate the data coverage notice:

  ```
  var coverageEl = document.getElementById('data-coverage-notice');
  if (coverageEl) {
    var total = 8; // Total model indicators (excluding ASX benchmark)
    var available = orderedIds.length;
    if (available < total) {
      coverageEl.textContent = 'Based on ' + available + ' of ' + total + ' indicators (more data coming soon)';
    } else {
      coverageEl.textContent = '';
    }
  }
  ```

  Place this logic after the `orderedIds` array is fully constructed but before the `if (orderedIds.length === 0)` check. The total count is 8 because ASX futures is excluded from the hawk score and rendered separately.
  </action>
  <verify>
  Reload the page and check:
  - Verdict text below the hero gauge reads "The economy is giving mixed signals. Interest rates are likely to stay where they are for now." (since score is 46, which falls in the 40-60 range)
  - Does NOT say "Economic indicators are broadly balanced"
  - Economic Indicators section shows "Based on 3 of 8 indicators (more data coming soon)" since only inflation, wages, and building_approvals have data
  - `grep -c 'innerHTML' public/js/interpretations.js public/js/gauge-init.js public/index.html` shows 0 for all files
  - All new DOM elements use createElement + textContent (safe methods)
  </verify>
  <done>
  Verdict text uses getPlainVerdict to produce actionable, ASIC-compliant language about interest rates. Data coverage notice shows "Based on 3 of 8 indicators (more data coming soon)" when indicators are missing. All DOM manipulation uses safe methods.
  </done>
</task>

</tasks>

<verification>
Full page verification after all tasks:

1. **Data quality guard**: Building approvals card shows "Building approvals data is currently being updated" instead of "-99.9% YoY"
2. **Plain English interpretations**: Inflation card says "Prices up 1.4% over the past year" not "CPI at 1.4% YoY"
3. **Why it matters**: Each of the 3 visible cards has an italic one-liner below the interpretation explaining relevance to rates
4. **Weight badges**: "High importance", "Medium importance", "Lower importance" labels; hover shows actual percentage
5. **Stale labels**: Wages card (220 days) shows "(7 months old -- newer data not yet available)" not "(stale)"
6. **Verdict**: "The economy is giving mixed signals. Interest rates are likely to stay where they are for now."
7. **Australian dates**: All source citations show "1 Dec 2025" format, not "2025-12-01"
8. **Coverage notice**: "Based on 3 of 8 indicators (more data coming soon)" visible in Economic Indicators section
9. **No innerHTML**: `grep -r 'innerHTML' public/js/interpretations.js public/js/gauge-init.js` returns nothing
10. **ASIC compliance**: No verdict text uses "will", "should", "we recommend", or directive language
</verification>

<success_criteria>
- All 8 items from the plan assignment are implemented
- Zero jargon in metric card text (no CPI, YoY, target band, weight %)
- Building approvals -99.9% never shown to users
- ASIC RG 244 compliance maintained in all text (verified by absence of directive language)
- All DOM manipulation uses safe methods (no innerHTML)
- Page loads without console errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-plain-english/06-02-SUMMARY.md`
</output>
