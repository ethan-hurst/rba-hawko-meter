---
phase: 01-foundation-data-pipeline
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/weekly-pipeline.yml
  - .github/workflows/daily-asx-futures.yml
autonomous: false
gap_closure: true

must_haves:
  truths:
    - "GitHub Actions workflows are validated and ready for execution"
    - "User has triggered at least one workflow run successfully"
    - "Automated commit from github-actions[bot] appears in git history"
  artifacts:
    - path: ".github/workflows/weekly-pipeline.yml"
      provides: "Weekly pipeline automation"
      min_lines: 30
    - path: ".github/workflows/daily-asx-futures.yml"
      provides: "Daily ASX futures automation"
      min_lines: 30
  key_links:
    - from: ".github/workflows/weekly-pipeline.yml"
      to: "pipeline/main.py"
      via: "python -m pipeline.main"
      pattern: "python -m pipeline.main"
    - from: ".github/workflows/daily-asx-futures.yml"
      to: "pipeline/ingest/asx_futures_scraper.py"
      via: "python -m pipeline.ingest.asx_futures_scraper"
      pattern: "python -m pipeline.ingest.asx_futures_scraper"
---

<objective>
Validate GitHub Actions workflows and guide user through first successful execution to close Gap 1 from verification.

Purpose: The verification report found that workflow files exist but have never been triggered. The phase goal is "System reliably ingests economic data weekly and commits it to Git" - without at least one successful automated run, this cannot be verified. This plan validates the workflow files, ensures they will succeed on first run, and guides the user through manual trigger.

Output: At least one successful GitHub Actions workflow run with automated data commit.
</objective>

<execution_context>
@/Users/annon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/annon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-data-pipeline/01-VERIFICATION.md
@.planning/phases/01-foundation-data-pipeline/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix GitHub Actions workflow files</name>
  <files>.github/workflows/weekly-pipeline.yml, .github/workflows/daily-asx-futures.yml</files>
  <action>
    Review both workflow files for correctness and potential issues that would prevent successful first run:

    1. **Verify action versions are current:**
       - `actions/checkout@v6` - verify this is a valid version. If v6 does not exist yet, downgrade to `actions/checkout@v4` (latest stable).
       - `actions/setup-python@v6` - same check. Downgrade to `actions/setup-python@v5` if v6 doesn't exist.
       - `stefanzweifel/git-auto-commit-action@v7` - verify this version exists. Downgrade to `@v5` if needed.

       To verify versions, check the GitHub Action marketplace or use:
       `gh api repos/actions/checkout/tags --jq '.[].name' | head -10`
       `gh api repos/actions/setup-python/tags --jq '.[].name' | head -10`
       `gh api repos/stefanzweifel/git-auto-commit-action/tags --jq '.[].name' | head -10`

    2. **Verify workflow structure:**
       - `permissions: contents: write` is present (required for git-auto-commit-action)
       - `workflow_dispatch:` is present (allows manual trigger)
       - Cron expressions are valid
       - Python version 3.11 is specified
       - `cache: 'pip'` is configured for faster runs
       - `pip install -r requirements.txt` step exists

    3. **Check for potential issues:**
       - The weekly pipeline runs `python -m pipeline.main` - verify this is the correct module path
       - The daily ASX runs `python -m pipeline.ingest.asx_futures_scraper` - verify this module has `if __name__ == '__main__'` block
       - The commit step uses `file_pattern: 'data/*.csv'` - verify this matches actual output files
       - The `[skip ci]` in commit message prevents infinite trigger loops

    4. **Fix any issues found.** If action versions need updating, update them. If other issues are found, fix them.

    5. **Verify the workflows are on the main branch** (cron schedules only trigger from the default branch):
       Run `git branch --show-current` to confirm we're on main.
       Run `git log --oneline -5` to see recent commits.
  </action>
  <verify>
    Run: `cat .github/workflows/weekly-pipeline.yml` - file exists and has valid YAML
    Run: `cat .github/workflows/daily-asx-futures.yml` - file exists and has valid YAML
    Run: `python -c "import yaml; yaml.safe_load(open('.github/workflows/weekly-pipeline.yml'))" 2>/dev/null || python3 -c "import sys; [print(l.rstrip()) for l in open('.github/workflows/weekly-pipeline.yml')]"` - YAML is valid
    Both files reference valid action versions that exist on GitHub.
  </verify>
  <done>
    Both workflow files are validated and any issues fixed. Action versions are confirmed valid. Workflow structure is correct for automated pipeline execution and data commit.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Validated GitHub Actions workflow files for the weekly data pipeline and daily ASX futures scraper. The workflows are ready for their first execution. All changes from the gap closure plans (including Building Approvals from plan 01-04) need to be pushed to the main branch before workflows can trigger.
  </what-built>
  <how-to-verify>
    1. **Push all changes to GitHub:**
       ```bash
       git push origin main
       ```

    2. **Trigger the weekly pipeline workflow manually:**
       - Go to your repository on GitHub
       - Click "Actions" tab
       - Select "Weekly Data Pipeline" from the left sidebar
       - Click "Run workflow" button (top right)
       - Select branch: main
       - Click "Run workflow"

    3. **Wait for the workflow to complete** (should take 1-3 minutes)

    4. **Verify the result:**
       - Workflow should show green checkmark (success) or yellow (partial - exit code 2)
       - A new commit should appear from "github-actions[bot]" with message "data: weekly pipeline update [skip ci]"
       - The data/*.csv files should be updated

    5. **Pull the automated commit locally:**
       ```bash
       git pull origin main
       ```
       Verify the automated commit exists in git log.

    6. **Optional: Also trigger the daily ASX futures workflow** (same process, select "Daily ASX Futures Scraper")
       Note: The ASX scraper may fail (exit code 2) since it's a placeholder - this is expected and acceptable.
  </how-to-verify>
  <resume-signal>Type "approved" if the workflow ran successfully and an automated commit appeared, or describe any issues encountered.</resume-signal>
</task>

</tasks>

<verification>
1. Both workflow YAML files are valid and use correct action versions
2. At least one successful workflow run visible in GitHub Actions tab
3. At least one automated commit from github-actions[bot] in git history
4. Data CSV files were updated by the automated pipeline
</verification>

<success_criteria>
- GitHub Actions workflows validated with correct action versions
- User successfully triggers at least one workflow run
- Automated commit from github-actions[bot] appears in repository
- Cron schedule confirmed working (or at minimum, manual trigger proven)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-pipeline/01-05-SUMMARY.md`
</output>
